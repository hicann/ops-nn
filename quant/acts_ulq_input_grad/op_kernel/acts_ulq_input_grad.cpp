/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file acts_ulq_input_grad_apt.cpp
 * \brief ActsULQInputGrad 算子 kernel 入口（arch35 / DAV_3510 架构）
 *
 * 一芯片一算子一入口：arch35 的唯一 __global__ 入口。
 *
 * 核函数参数顺序（固定）：3 输入 + 1 输出 + workspace + tiling。
 *
 * dtype 由 def 驱动：构建系统按 def 的 DataType profile 生成 DTYPE_<输入名大写> 宏
 *   （DTYPE_Y_GRAD / DTYPE_CLAMP_MIN_MASK），在入口处实例化 op 类的 T / MT 模板实参。
 *
 * 模板参数（与 acts_ulq_input_grad_tiling_key.h 的 ASCENDC_TPL_ARGS_DECL 对应）：
 *   - BUFFER_MODE: 缓冲模式（0=单缓冲, 1=双缓冲）
 */

#include "arch35/acts_ulq_input_grad.h"

template <int BUFFER_MODE>
__global__ __aicore__ void acts_ulq_input_grad(GM_ADDR y_grad, GM_ADDR clamp_min_mask, GM_ADDR clamp_max_mask,
                                               GM_ADDR x_grad, GM_ADDR workspace, GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(ActsULQInputGradTilingData);
    GET_TILING_DATA_WITH_STRUCT(ActsULQInputGradTilingData, tilingData, tiling);
    // dtype 由 def 驱动：DTYPE_Y_GRAD=y_grad/x_grad dtype（T），DTYPE_CLAMP_MIN_MASK=mask dtype（MT）
    NsActsULQInputGrad::ActsULQInputGrad<DTYPE_Y_GRAD, DTYPE_CLAMP_MIN_MASK, BUFFER_MODE> op;
    op.Init(y_grad, clamp_min_mask, clamp_max_mask, x_grad, &tilingData);
    op.Process();
}
