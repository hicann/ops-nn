/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */
/* Generated By CANNBot */

/*!
 * \file npu_clear_float_status_infershape.cpp
 * \brief Shape/Format inference for npu_clear_float_status
 *
 * InferDataType 已迁移至 op_graph/npu_clear_float_status_graph_infer.cpp (IMPL_OP 注册)。
 * 本文件仅保留 InferShape + InferFormat (IMPL_OP_INFERSHAPE 注册)。
 *
 * InferShape: 输出 shape 固定为 (8,)，不依赖输入 addr 的 shape
 * InferFormat: 输出 format 固定为 ND
 * infershape 侧校验: addr dtype == float32
 */

#include "graph/types.h"
#include "graph/utils/type_utils.h"
#include "register/op_impl_registry.h"
#include "log/log.h"
#include "npu_clear_float_status_common.h"

using namespace ge;

namespace ops {

// infershape 侧仅校验 input addr，output data 校验由 tiling 阶段负责
static ge::graphStatus InferShapeValidateDtype(gert::InferShapeContext* context)
{
    auto addrDesc = context->GetInputDesc(NpuCfs::ADDR_IDX);
    OP_CHECK_NULL_WITH_CONTEXT(context, addrDesc);
    OP_CHECK_IF(addrDesc->GetDataType() != ge::DT_FLOAT,
                OP_LOGE_FOR_INVALID_DTYPE_WITH_REASON(context->GetNodeName(), "addr",
                                                      ge::TypeUtils::DataTypeToSerialString(addrDesc->GetDataType()),
                                                      "The dtype of addr must be FLOAT"),
                return ge::GRAPH_FAILED);
    return ge::GRAPH_SUCCESS;
}

static ge::graphStatus InferShapeValidateInputs(gert::InferShapeContext* context)
{
    OP_CHECK_IF(InferShapeValidateDtype(context) != ge::GRAPH_SUCCESS,
                OP_LOGE(context, "InferShapeValidateDtype failed"), return ge::GRAPH_FAILED);
    return ge::GRAPH_SUCCESS;
}

// 输出 shape 固定为 (8,)
static ge::graphStatus InferShapeNPUClearFloatStatus(gert::InferShapeContext* context)
{
    OP_LOGD(context->GetNodeName(), "Begin to do InferShapeNPUClearFloatStatus");

    OP_CHECK_IF(InferShapeValidateInputs(context) != ge::GRAPH_SUCCESS,
                OP_LOGE(context, "InferShapeValidateInputs failed"), return ge::GRAPH_FAILED);

    gert::Shape* dataShape = context->GetOutputShape(NpuCfs::DATA_IDX);
    OP_CHECK_NULL_WITH_CONTEXT(context, dataShape);
    dataShape->SetDimNum(NpuCfs::EXPECTED_DIM_NUM);
    dataShape->SetDim(0, NpuCfs::OUTPUT_DIM);

    OP_LOGD(context->GetNodeName(), "End to do InferShapeNPUClearFloatStatus");
    return GRAPH_SUCCESS;
}

// InferFormat — 输出 format 固定为 ND
static ge::graphStatus InferFormatNPUClearFloatStatus(gert::InferFormatContext* context)
{
    OP_LOGD(context->GetNodeName(), "Begin to do InferFormatNPUClearFloatStatus");
    auto* outputFormat = context->GetOutputFormat(NpuCfs::DATA_IDX);
    OP_CHECK_NULL_WITH_CONTEXT(context, outputFormat);
    outputFormat->SetOriginFormat(ge::FORMAT_ND);
    outputFormat->SetStorageFormat(ge::FORMAT_ND);
    OP_LOGD(context->GetNodeName(), "End to do InferFormatNPUClearFloatStatus");
    return GRAPH_SUCCESS;
}

// InferDataType 已迁移至 op_graph/npu_clear_float_status_graph_infer.cpp
IMPL_OP_INFERSHAPE(NPUClearFloatStatus)
    .InferShape(InferShapeNPUClearFloatStatus)
    .InferFormat(InferFormatNPUClearFloatStatus);
} // namespace ops
