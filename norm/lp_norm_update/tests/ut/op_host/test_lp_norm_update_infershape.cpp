/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file test_lp_norm_update_infershape.cpp
 * \brief LpNormUpdate InferShape 独立逻辑测试
 *
 * 测试策略：从 lp_norm_update_infershape.cpp 复制 static 函数逻辑，
 * 不依赖 CANN faker 框架，使用独立编译验证形状推导正确性。
 *
 * InferShape 逻辑：elementwise 操作，output_shape = input_shape
 */

#include <cstdint>
#include <cstdio>
#include <vector>
#include <string>

// ===================== 测试框架（轻量级，无外部依赖） =====================

static int g_testCount = 0;
static int g_passCount = 0;
static int g_failCount = 0;
static std::string g_currentTest;

#define TEST_BEGIN(name)                                \
    do {                                                \
        g_currentTest = (name);                         \
        g_testCount++;                                  \
        printf("  [RUN ] %s\n", g_currentTest.c_str()); \
    } while (0)

#define TEST_END()                                      \
    do {                                                \
        printf("  [PASS] %s\n", g_currentTest.c_str()); \
        g_passCount++;                                  \
    } while (0)

#define ASSERT_EQ(a, b)                                                                                            \
    do {                                                                                                           \
        if (!((a) == (b))) {                                                                                       \
            printf("  [FAIL] %s at line %d: ASSERT_EQ(%s, %s) failed\n", g_currentTest.c_str(), __LINE__, #a, #b); \
            g_failCount++;                                                                                         \
            return;                                                                                                \
        }                                                                                                          \
    } while (0)

#define ASSERT_TRUE(cond)                                                                                       \
    do {                                                                                                        \
        if (!(cond)) {                                                                                          \
            printf("  [FAIL] %s at line %d: ASSERT_TRUE(%s) failed\n", g_currentTest.c_str(), __LINE__, #cond); \
            g_failCount++;                                                                                      \
            return;                                                                                             \
        }                                                                                                       \
    } while (0)

// ===================== 被测逻辑（从源文件复制） =====================

using Shape = std::vector<int64_t>;

/*!
 * \brief InferShape4LpNormUpdate 逻辑复制
 *
 * 原始逻辑（lp_norm_update_infershape.cpp）：
 *   const gert::Shape* input_shape = context->GetInputShape(0);
 *   gert::Shape* output_shape = context->GetOutputShape(0);
 *   *output_shape = *input_shape;
 *
 * elementwise 操作：输出形状 = 输入形状
 */
static Shape InferShape4LpNormUpdate(const Shape& inputShape) { return inputShape; }

// ===================== 测试用例 =====================

// 测试 y.shape = x.shape（基本 2D 场景）
void test_infershape_basic_2d()
{
    TEST_BEGIN("infershape_basic_2d");
    Shape inputShape = {8, 16};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape.size(), inputShape.size());
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试空输入（0 元素）
void test_infershape_empty_input()
{
    TEST_BEGIN("infershape_empty_input");
    Shape inputShape = {0};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape.size(), 1U);
    ASSERT_EQ(outputShape[0], 0);
    TEST_END();
}

// 测试 0D（标量）
void test_infershape_0d_scalar()
{
    TEST_BEGIN("infershape_0d_scalar");
    Shape inputShape = {}; // 0D = 标量
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape.size(), 0U);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试 1D
void test_infershape_1d()
{
    TEST_BEGIN("infershape_1d");
    Shape inputShape = {256};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试 3D
void test_infershape_3d()
{
    TEST_BEGIN("infershape_3d");
    Shape inputShape = {4, 1, 8};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试 4D
void test_infershape_4d()
{
    TEST_BEGIN("infershape_4d");
    Shape inputShape = {2, 3, 4, 8};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试 5D
void test_infershape_5d()
{
    TEST_BEGIN("infershape_5d");
    Shape inputShape = {1, 2, 3, 4, 5};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试 8D（最大维度）
void test_infershape_8d()
{
    TEST_BEGIN("infershape_8d");
    Shape inputShape = {1, 1, 1, 1, 1, 1, 1, 1};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape.size(), 8U);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试含 -1（动态维度）
void test_infershape_dynamic_dim()
{
    TEST_BEGIN("infershape_dynamic_dim");
    Shape inputShape = {-1, 16};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    ASSERT_EQ(outputShape[0], -1);
    ASSERT_EQ(outputShape[1], 16);
    TEST_END();
}

// 测试全 -2（未知维度）
void test_infershape_unknown_shape()
{
    TEST_BEGIN("infershape_unknown_shape");
    Shape inputShape = {-2};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    TEST_END();
}

// 测试大维度
void test_infershape_large_shape()
{
    TEST_BEGIN("infershape_large_shape");
    Shape inputShape = {1024, 1024};
    Shape outputShape = InferShape4LpNormUpdate(inputShape);
    ASSERT_EQ(outputShape, inputShape);
    ASSERT_EQ(outputShape[0], 1024);
    ASSERT_EQ(outputShape[1], 1024);
    TEST_END();
}

// ===================== 主函数 =====================

int main()
{
    printf("=== LpNormUpdate InferShape UT ===\n\n");

    test_infershape_basic_2d();
    test_infershape_empty_input();
    test_infershape_0d_scalar();
    test_infershape_1d();
    test_infershape_3d();
    test_infershape_4d();
    test_infershape_5d();
    test_infershape_8d();
    test_infershape_dynamic_dim();
    test_infershape_unknown_shape();
    test_infershape_large_shape();

    printf("\n=== Summary ===\n");
    printf("Total: %d, Pass: %d, Fail: %d\n", g_testCount, g_passCount, g_failCount);

    return (g_failCount == 0) ? 0 : 1;
}
