/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <array>
#include <vector>
#include <iostream>
#include <string>
#include <cstdint>
#include "gtest/gtest.h"
#include "tikicpulib.h"
#include "data_utils.h"
#include "kernel_ut_data_helper.h"
#include "kernel_ut_data_executor.h"

#define __RELU6_D_TILING_KEY_H__
#include "../../../../op_kernel/arch35/relu6_d_tiling_data.h"
#include "../../../../op_kernel/arch35/relu6_d.h"

using namespace std;

template <typename T>
static void relu6_d_kernel(GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling)
{
    const Relu6DTilingData* tilingData = reinterpret_cast<const Relu6DTilingData*>(tiling);
    NsRelu6D::KernelRelu6D<T> op;
    op.Init(x, y, tilingData);
    op.Process();
}

class relu6_d_test : public testing::Test {
protected:
    static void SetUpTestCase() { cout << "relu6_d_test SetUp\n" << endl; }
    static void TearDownTestCase()
    {
        cout << "relu6_d TearDown\n" << endl;
        kernel_ut::CleanGeneratedBinFiles("./relu6_d_data");
    }
};

// 填充 TilingData（单核单轮场景：dim0=N, blockFormer=N, blockNum=1, ubFormer=N, 单次循环）
static void FillTilingData(Relu6DTilingData* tilingData, int64_t dataNum, float scale)
{
    tilingData->dim0 = dataNum;
    tilingData->coreNum = 1;
    tilingData->blockFormer = dataNum;
    tilingData->blockNum = 1;
    tilingData->ubFormer = dataNum;
    tilingData->ubLoopOfFormerBlock = 1;
    tilingData->ubTailOfFormerBlock = dataNum;
    tilingData->ubLoopOfTailBlock = 1;
    tilingData->ubTailOfTailBlock = dataNum;
    tilingData->upperThreshold = 6.0f * scale;
}

TEST_F(relu6_d_test, test_case_fp32_small)
{
    size_t dataNum = 64;
    size_t xByteSize = dataNum * sizeof(float);
    size_t yByteSize = dataNum * sizeof(float);
    size_t tiling_data_size = sizeof(Relu6DTilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/relu6_d/tests/ut/op_kernel/relu6_d_data", "relu6_d_data");
    kernel_ut::RunGenData("./relu6_d_data", {"'(64)'", "float32"});

    std::string path = kernel_ut::GetTestWorkDir();

    Relu6DTilingData* tilingData = reinterpret_cast<Relu6DTilingData*>(tiling);
    FillTilingData(tilingData, static_cast<int64_t>(dataNum), 1.0f);

    ReadFile(path + "/relu6_d_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelRelu6D = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        relu6_d_kernel<float>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(2);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelRelu6D, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/relu6_d_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(relu6_d_test, test_case_fp16_small)
{
    size_t dataNum = 128;
    size_t xByteSize = dataNum * sizeof(uint16_t);
    size_t yByteSize = dataNum * sizeof(uint16_t);
    size_t tiling_data_size = sizeof(Relu6DTilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/relu6_d/tests/ut/op_kernel/relu6_d_data", "relu6_d_data");
    kernel_ut::RunGenData("./relu6_d_data", {"'(128)'", "float16"});

    std::string path = kernel_ut::GetTestWorkDir();

    Relu6DTilingData* tilingData = reinterpret_cast<Relu6DTilingData*>(tiling);
    FillTilingData(tilingData, static_cast<int64_t>(dataNum), 1.0f);

    ReadFile(path + "/relu6_d_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelRelu6D = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        relu6_d_kernel<half>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelRelu6D, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/relu6_d_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(relu6_d_test, test_case_int32_small)
{
    size_t dataNum = 64;
    size_t xByteSize = dataNum * sizeof(int32_t);
    size_t yByteSize = dataNum * sizeof(int32_t);
    size_t tiling_data_size = sizeof(Relu6DTilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/relu6_d/tests/ut/op_kernel/relu6_d_data", "relu6_d_data");
    kernel_ut::RunGenData("./relu6_d_data", {"'(64)'", "int32"});

    std::string path = kernel_ut::GetTestWorkDir();

    Relu6DTilingData* tilingData = reinterpret_cast<Relu6DTilingData*>(tiling);
    FillTilingData(tilingData, static_cast<int64_t>(dataNum), 1.0f);

    ReadFile(path + "/relu6_d_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelRelu6D = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        relu6_d_kernel<int32_t>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(3);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelRelu6D, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/relu6_d_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(relu6_d_test, test_case_bf16_small)
{
    size_t dataNum = 128;
    size_t xByteSize = dataNum * sizeof(uint16_t);
    size_t yByteSize = dataNum * sizeof(uint16_t);
    size_t tiling_data_size = sizeof(Relu6DTilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/relu6_d/tests/ut/op_kernel/relu6_d_data", "relu6_d_data");
    kernel_ut::RunGenData("./relu6_d_data", {"'(128)'", "bfloat16"});

    std::string path = kernel_ut::GetTestWorkDir();

    Relu6DTilingData* tilingData = reinterpret_cast<Relu6DTilingData*>(tiling);
    FillTilingData(tilingData, static_cast<int64_t>(dataNum), 1.0f);

    ReadFile(path + "/relu6_d_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelRelu6D = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        relu6_d_kernel<bfloat16_t>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(1);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelRelu6D, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/relu6_d_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}

TEST_F(relu6_d_test, test_case_fp32_multi_loop)
{
    size_t dataNum = 512;
    size_t xByteSize = dataNum * sizeof(float);
    size_t yByteSize = dataNum * sizeof(float);
    size_t tiling_data_size = sizeof(Relu6DTilingData);

    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(16 * 1024 * 1024);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);
    uint32_t blockDim = 1;
    kernel_ut::SetupTestEnvironment("activation/relu6_d/tests/ut/op_kernel/relu6_d_data", "relu6_d_data");
    kernel_ut::RunGenData("./relu6_d_data", {"'(512)'", "float32"});

    std::string path = kernel_ut::GetTestWorkDir();

    Relu6DTilingData* tilingData = reinterpret_cast<Relu6DTilingData*>(tiling);
    FillTilingData(tilingData, static_cast<int64_t>(dataNum), 1.0f);
    tilingData->ubFormer = 256;
    tilingData->ubLoopOfFormerBlock = 2;
    tilingData->ubTailOfFormerBlock = 256;
    tilingData->ubLoopOfTailBlock = 2;
    tilingData->ubTailOfTailBlock = 256;

    ReadFile(path + "/relu6_d_data/input_x.bin", xByteSize, x, xByteSize);
    auto KernelRelu6D = [](GM_ADDR x, GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling) {
        relu6_d_kernel<float>(x, y, workspace, tiling);
    };
    ICPU_SET_TILING_KEY(2);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(KernelRelu6D, blockDim, x, y, workspace, (uint8_t*)(tilingData));
    WriteFile(path + "/relu6_d_data/output.bin", y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}
