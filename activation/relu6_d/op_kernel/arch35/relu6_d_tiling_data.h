/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file relu6_d_tiling_data.h
 * \brief Relu6D TilingData 结构体定义（RegBase 标准 C++ struct）
 *
 * EleWise 1D 直算，workspace=0。host 与 kernel 共用，纯 POD（无成员函数/指针/虚函数），
 * 禁止使用废弃宏 BEGIN_TILING_DATA_DEF。
 *
 * 字段职责：
 *   dim0                 展平元素总数 = ∏(x.shape)；rank=0 → 1，空 Tensor → 0
 *   coreNum              求 blockFormer 的除数（上限 GetCoreNumAiv()），非最终工作核数
 *   blockFormer          每核基础元素数（对齐到 512 元素 ELEM_ALIGN_FACTOR）
 *   blockNum             真正发起 GM 读写的工作核数 = ⌈dim0/blockFormer⌉
 *   ubFormer             每个 UB 块基础元素数（对齐到 256B）
 *   ubLoopOfFormerBlock  首 block 的 UB 循环次数
 *   ubTailOfFormerBlock  首 block 尾部元素数
 *   ubLoopOfTailBlock    尾 block 的 UB 循环次数
 *   ubTailOfTailBlock    尾 block 尾部元素数
 *   upperThreshold       上界阈值 = 6*scale（Host 计算，float 载体；kernel 按 T 转标量）
 */

#ifndef _RELU6_D_TILING_DATA_H_
#define _RELU6_D_TILING_DATA_H_

struct Relu6DTilingData {
    // === 多核切分参数 ===
    int64_t dim0 = 0;        // 展平元素总数
    int32_t coreNum = 0;     // 求 blockFormer 的除数（非工作核数）
    int64_t blockFormer = 0; // 每核基础元素数（512 元素对齐）
    int64_t blockNum = 0;    // 工作核数 = ⌈dim0/blockFormer⌉

    // === UB 切分参数 ===
    int64_t ubFormer = 0;            // 每个 UB 块基础元素数（256B 对齐）
    int64_t ubLoopOfFormerBlock = 0; // 首 block UB 循环次数
    int64_t ubTailOfFormerBlock = 0; // 首 block 尾部元素数
    int64_t ubLoopOfTailBlock = 0;   // 尾 block UB 循环次数
    int64_t ubTailOfTailBlock = 0;   // 尾 block 尾部元素数

    // === 标量参数 ===
    float upperThreshold = 6.0f; // 上界阈值 = 6*scale（kernel 侧按 T 转标量传入 Mins）
};

#endif // _RELU6_D_TILING_DATA_H_
