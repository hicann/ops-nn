/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file smooth_l1_loss_grad_tiling_data.h
 * \brief SmoothL1LossGrad TilingData 结构体定义（RegBase 标准 C++ struct）
 *
 * SmoothL1LossGradTilingData 结构体定义（EleWise 1D，workspace=0）。
 * host 与 kernel 共用，纯 POD（无成员函数/指针/虚函数），禁止使用废弃宏
 * BEGIN_TILING_DATA_DEF。
 *
 * 字段职责：
 *   totalLength   展平元素总数 = ∏(predict.shape)；rank=0 → 1，空 Tensor → 0
 *   coreNum       求 blockFormer 的除数（上限 GetCoreNumAiv()），非最终工作核数
 *   blockFormer   每核基础元素数（对齐到 512 元素 ELEM_ALIGN_FACTOR）
 *   blockNum      真正发起 GM 读写的工作核数 = ⌈totalLength/blockFormer⌉
 *   blockTail     尾核处理元素数 = totalLength - blockFormer*(blockNum-1)（int64 防 uint32 回绕）
 *   ubFormer      每个 UB 块基础元素数（对齐到 256B）
 *   sigma         平滑阈值 σ（Step 2 Mins 上截断，σ > 0）
 *   negSigma      -σ（Step 3 Maxs 下截断，Host 预计算）
 *   recipSigma    1/σ（Step 4 Muls 缩放，Host 除法转乘法预计算）
 */

#ifndef _SMOOTH_L1_LOSS_GRAD_TILING_DATA_H_
#define _SMOOTH_L1_LOSS_GRAD_TILING_DATA_H_

struct SmoothL1LossGradTilingData {
    // === 多核切分参数 ===
    int64_t totalLength = 0; // 展平元素总数
    int32_t coreNum = 0;     // 求 blockFormer 的除数（非工作核数）
    int64_t blockFormer = 0; // 每核基础元素数（512 元素对齐）
    int64_t blockNum = 0;    // 工作核数 = ⌈totalLength/blockFormer⌉
    int64_t blockTail = 0;   // 尾核处理元素数（int64 防 uint32 回绕）

    // === UB 切分参数 ===
    int64_t ubFormer = 0; // 每个 UB 块基础元素数（256B 对齐）

    // === 标量参数（Host 预计算） ===
    float sigma = 1.0f;      // 平滑阈值 σ（σ > 0）
    float negSigma = -1.0f;  // -σ（Step 3 Maxs 下截断）
    float recipSigma = 1.0f; // 1/σ（Step 4 Muls 缩放，除法转乘法）
};

#endif // _SMOOTH_L1_LOSS_GRAD_TILING_DATA_H_
