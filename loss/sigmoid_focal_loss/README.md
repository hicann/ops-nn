<!-- Generated By CANNBot -->
# SigmoidFocalLoss

## 产品支持情况

| 产品 | 是否支持 |
| ---- | :----: |
| Ascend 950PR/Ascend 950DT | √ |
| Atlas A3 训练系列产品/Atlas A3 推理系列产品 | √ |
| Atlas A2 训练系列产品/Atlas A2 推理系列产品 | √ |
| Atlas 200I/500 A2 推理产品 | √ |
| Atlas 推理系列产品 | √ |
| Atlas 训练系列产品 | √ |

## 功能说明

- 算子功能：SigmoidFocalLoss面向目标检测等二分类或逐类别独立二分类训练场景，将logits的Sigmoid二元交叉熵、类别平衡因子和难样本聚焦因子组合为逐元素损失；可选输入`weight`在归约前进行逐元素加权。

- 计算公式：设$x=\operatorname{FP32}(pred)$、$t=\operatorname{FP32}(target)$、$p=\sigma(x)$、$\varepsilon=1.17549435\times10^{-38}$，则每个元素的损失为：

$$
\operatorname{BCE}(x,t)=-t\log(\max(p,\varepsilon))-(1-t)\log(\max(1-p,\varepsilon))
$$

$$
p_t=(1-p)t+p(1-t),\quad a_t=\alpha t+(1-\alpha)(1-t)
$$

$$
\ell=\operatorname{BCE}(x,t)\,a_t\max(p_t,\varepsilon)^{\gamma}\times
\begin{cases}
weight,&\text{提供weight},\\
1,&\text{未提供weight}.
\end{cases}
$$

- 输出规则：`reduction`为`none`时输出逐元素损失，为`sum`时输出损失之和，为`mean`时输出损失的算术平均。输出dtype与`pred`相同。

## 参数说明

<table style="table-layout: fixed; width: 1576px">
<colgroup>
<col style="width: 170px">
<col style="width: 170px">
<col style="width: 200px">
<col style="width: 200px">
<col style="width: 170px">
</colgroup>
<thead>
<tr><th>参数名</th><th>输入/输出/属性</th><th>描述</th><th>数据类型</th><th>数据格式</th></tr>
</thead>
<tbody>
<tr><td>pred</td><td>输入</td><td>形状为[N,C]的logits，对应公式中的pred。</td><td>FLOAT16、FLOAT</td><td>ND</td></tr>
<tr><td>target</td><td>输入</td><td>与pred同形的二值标签，对应公式中的target。</td><td>INT32</td><td>ND</td></tr>
<tr><td>weight</td><td>可选输入</td><td>与pred同形、同dtype的逐元素样本权重；未提供时不进行权重乘法。</td><td>FLOAT16、FLOAT</td><td>ND</td></tr>
<tr><td>y</td><td>输出</td><td>Focal Loss结果；none时与pred同形，sum或mean时为标量。</td><td>FLOAT16、FLOAT</td><td>ND</td></tr>
<tr><td>gamma</td><td>可选属性</td><td>聚焦指数，取值范围为[0.0,5.0]，默认2.0。</td><td>FLOAT</td><td>-</td></tr>
<tr><td>alpha</td><td>可选属性</td><td>正负类别平衡系数，取值范围为[0.0,1.0]，默认0.25。</td><td>FLOAT</td><td>-</td></tr>
<tr><td>reduction</td><td>可选属性</td><td>归约方式，支持mean、sum、none，默认mean。</td><td>STRING</td><td>-</td></tr>
</tbody>
</table>

## 约束说明

- `pred`、`target`和已提供的`weight`必须为完全相同的rank-2 shape，不支持广播。
- `target`的元素值应属于二值集合{0,1}。
- `weight`存在时必须与`pred`的dtype相同，输出`y`的dtype与`pred`相同。
- `gamma`和`alpha`必须为有限值，并分别位于闭区间[0.0,5.0]和[0.0,1.0]。
- rank-2空Tensor受支持：`none`返回同形空Tensor，`sum`返回标量0，`mean`返回标量NaN。

## 调用说明

<table style="table-layout: fixed; width: 1000px">
<colgroup><col style="width: 180px"><col style="width: 120px"><col style="width: 700px"></colgroup>
<thead><tr><th>调用方式</th><th>调用样例</th><th>说明</th></tr></thead>
<tbody>
<tr><td>GE图模式</td><td><a href="examples/arch35/test_geir_sigmoid_focal_loss.cpp">静态shape样例</a></td><td>通过<a href="op_graph/sigmoid_focal_loss_proto.h">算子IR</a>构图调用SigmoidFocalLoss算子。</td></tr>
</tbody>
</table>
