/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file test_smooth_l1_loss_tiling.cpp
 * \brief
 */

#include <iostream>
#include <vector>
#include "log/log.h"
#include <gtest/gtest.h>
#include "register/op_impl_registry.h"
#include "array_ops.h"
#include "ut_op_util.h"
#include "kernel_run_context_facker.h"
#include "test_cube_util.h"
#include "exe_graph/runtime/storage_format.h"
#include "exe_graph/runtime/storage_shape.h"
#include "loss/smooth_l1_loss/op_host/arch35/smooth_l1_loss_tiling_arch35.h"
#include "loss/smooth_l1_loss/op_kernel/arch35/smooth_l1_loss_tilingdata.h"
using namespace std;
using namespace ge;
using namespace ut_util;

class SmoothL1LossTiling : public testing::Test {
protected:
    static void SetUpTestCase() { std::cout << "SmoothL1LossTiling SetUp" << std::endl; }

    static void TearDownTestCase() { std::cout << "SmoothL1LossTiling TearDown" << std::endl; }
};

static string TilingData2Str(const gert::TilingData* tiling_data)
{
    auto data = tiling_data->GetData();
    string result;
    for (size_t i = 0; i < tiling_data->GetDataSize(); i += sizeof(int64_t)) {
        result += std::to_string((reinterpret_cast<const int64_t*>(tiling_data->GetData())[i / sizeof(int64_t)]));
        result += " ";
    }

    return result;
}

static void InitPlatForm(fe::PlatFormInfos& platFormInfo, map<string, string>& socInfos,
                         map<string, string>& aicoreSpec, map<string, string>& intrinsics,
                         map<string, string>& socVersion)
{
    string hardwareInfo = R"({
        "hardware_info": {"UB_SIZE": 253952, "CORE_NUM": 64, "socVersion": "Ascend950"}
                          })";
    GetPlatFormInfos(hardwareInfo.c_str(), socInfos, aicoreSpec, intrinsics, socVersion);

    platFormInfo.Init();
}

static void DoSmoothL1LossTilingCase(std::initializer_list<int64_t>& predictShape,
                                     std::initializer_list<int64_t>& labelShape,
                                     std::initializer_list<int64_t>& outputShape, ge::DataType inputDtype, float sigma,
                                     int64_t expectTotalNum, int64_t expectBlockDim, int64_t expectWorkspace)
{
    fe::PlatFormInfos platFormInfo;
    map<string, string> socInfos;
    map<string, string> aicoreSpec;
    map<string, string> intrinsics;
    map<string, string> socVersion = {{"Short_SoC_version", "Ascend950"}, {"NpuArch", "3510"}};
    InitPlatForm(platFormInfo, socInfos, aicoreSpec, intrinsics, socVersion);

    optiling::SmoothL1LossCompileInfo compileInfo;
    compileInfo.coreNum = 64;
    compileInfo.ubSize = 253952;
    std::string opType("SmoothL1Loss");
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str()), nullptr);

    string compileInfoStr = R"({
        "device_id": null})";

    auto kernelHolder = gert::KernelRunContextFaker()
                            .KernelIONum(2, 1)
                            .Inputs({const_cast<char*>(compileInfoStr.c_str()), reinterpret_cast<void*>(&platFormInfo)})
                            .Outputs({&compileInfo})
                            .Build();

    ASSERT_TRUE(kernelHolder.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->Init());
    kernelHolder.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("SoCInfo", socInfos);
    kernelHolder.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreSpec", aicoreSpec);
    kernelHolder.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    kernelHolder.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap",
                                                                                           intrinsics);
    kernelHolder.GetContext<gert::TilingParseContext>()->GetPlatformInfo()->SetPlatformRes("version", socVersion);
    auto tilingParseFunc = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling_parse;
    ASSERT_EQ(tilingParseFunc(kernelHolder.GetContext<gert::KernelContext>()), ge::GRAPH_SUCCESS);

    gert::StorageShape xShape = {predictShape, predictShape};
    gert::StorageShape aShape = {labelShape, labelShape};
    gert::StorageShape oSahpe = {outputShape, outputShape};
    auto tilingFunc = gert::OpImplRegistry::GetInstance().GetOpImpl(opType.c_str())->tiling;

    auto workspaceSizeHoler = gert::ContinuousVector::Create<size_t>(16 * 4096);
    auto wsSize = reinterpret_cast<gert::ContinuousVector*>(workspaceSizeHoler.get());
    auto param = gert::TilingData::CreateCap(4096);
    ASSERT_NE(param, nullptr);

    auto holder = gert::TilingContextFaker()
                      .NodeIoNum(2, 1)
                      .IrInstanceNum({1, 1})
                      .InputShapes({&xShape, &aShape})
                      .OutputShapes({&oSahpe})
                      .CompileInfo(&compileInfo)
                      .PlatformInfo(reinterpret_cast<char*>(&platFormInfo))
                      .NodeInputTd(0, inputDtype, ge::FORMAT_ND, ge::FORMAT_ND)
                      .NodeInputTd(1, inputDtype, ge::FORMAT_ND, ge::FORMAT_ND)
                      .NodeOutputTd(0, inputDtype, ge::FORMAT_ND, ge::FORMAT_ND)
                      .NodeAttrs({{"sigma", Ops::NN::AnyValue::CreateFrom<float>(sigma)}})
                      .TilingData(param.get())
                      .Workspace(wsSize)
                      .Build();

    gert::TilingContext* tiling_context = holder.GetContext<gert::TilingContext>();
    ASSERT_NE(tiling_context->GetPlatformInfo(), nullptr);
    holder.GetContext<gert::TilingContext>()->GetPlatformInfo()->SetPlatformRes("SoCInfo", socInfos);
    holder.GetContext<gert::TilingContext>()->GetPlatformInfo()->SetPlatformRes("AICoreSpec", aicoreSpec);
    holder.GetContext<gert::TilingContext>()->GetPlatformInfo()->SetCoreNumByCoreType("AICore");
    holder.GetContext<gert::TilingContext>()->GetPlatformInfo()->SetPlatformRes("AICoreintrinsicDtypeMap", intrinsics);
    holder.GetContext<gert::TilingContext>()->GetPlatformInfo()->SetPlatformRes("version", socVersion);

    EXPECT_EQ(tilingFunc(tiling_context), ge::GRAPH_SUCCESS);

    auto tilingData = tiling_context->GetRawTilingData();
    ASSERT_NE(tilingData, nullptr);
    auto smoothTiling = reinterpret_cast<const SmoothL1Loss::SmoothL1LossTilingData*>(tilingData->GetData());
    ASSERT_NE(smoothTiling, nullptr);
    EXPECT_EQ(smoothTiling->totalNum, expectTotalNum);
    EXPECT_EQ(tiling_context->GetBlockDim(), expectBlockDim);
    auto workspaceSizes = tiling_context->GetWorkspaceSizes(1);
    ASSERT_NE(workspaceSizes, nullptr);
    EXPECT_EQ(workspaceSizes[0], static_cast<size_t>(expectWorkspace));
}

TEST_F(SmoothL1LossTiling, smooth_l1_loss_tiling_fp32)
{
    std::initializer_list<int64_t> predictShape = {2048, 1, 48};
    std::initializer_list<int64_t> labelShape = {2048, 1, 48};
    std::initializer_list<int64_t> outputShape = {2048, 1, 48};
    float sigma = 1.0;

    DoSmoothL1LossTilingCase(predictShape, labelShape, outputShape, ge::DT_FLOAT, sigma, 98304, 64, 0);
}

TEST_F(SmoothL1LossTiling, smooth_l1_loss_tiling_fp16)
{
    std::initializer_list<int64_t> predictShape = {64, 128};
    std::initializer_list<int64_t> labelShape = {64, 128};
    std::initializer_list<int64_t> outputShape = {64, 128};
    float sigma = 0.5;

    DoSmoothL1LossTilingCase(predictShape, labelShape, outputShape, ge::DT_FLOAT16, sigma, 8192, 64, 0);
}

TEST_F(SmoothL1LossTiling, smooth_l1_loss_tiling_bf16)
{
    std::initializer_list<int64_t> predictShape = {32, 64};
    std::initializer_list<int64_t> labelShape = {32, 64};
    std::initializer_list<int64_t> outputShape = {32, 64};
    float sigma = 2.0;

    DoSmoothL1LossTilingCase(predictShape, labelShape, outputShape, ge::DT_BF16, sigma, 2048, 64, 0);
}

TEST_F(SmoothL1LossTiling, smooth_l1_loss_tiling_sigma_zero)
{
    std::initializer_list<int64_t> predictShape = {256};
    std::initializer_list<int64_t> labelShape = {256};
    std::initializer_list<int64_t> outputShape = {256};
    float sigma = 0.0;

    DoSmoothL1LossTilingCase(predictShape, labelShape, outputShape, ge::DT_FLOAT, sigma, 256, 8, 0);
}

TEST_F(SmoothL1LossTiling, smooth_l1_loss_tiling_empty_tensor)
{
    std::initializer_list<int64_t> predictShape = {0, 4, 8};
    std::initializer_list<int64_t> labelShape = {0, 4, 8};
    std::initializer_list<int64_t> outputShape = {0, 4, 8};
    float sigma = 1.0;

    DoSmoothL1LossTilingCase(predictShape, labelShape, outputShape, ge::DT_FLOAT, sigma, 0, 1, 0);
}
