/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */
/* Generated By CANNBot */

#include <iostream>
#include <gtest/gtest.h>
#include "tiling_context_faker.h"
#include "tiling_case_executor.h"
#include "../../../../op_kernel/arch35/roi_pooling_tiling_data.h"

using namespace std;
using namespace ge;

class RoiPoolingTiling : public testing::Test {
protected:
    static void SetUpTestCase() { std::cout << "RoiPoolingTiling SetUp" << std::endl; }

    static void TearDownTestCase() { std::cout << "RoiPoolingTiling TearDown" << std::endl; }
};

TEST_F(RoiPoolingTiling, roi_pooling_float32_small_shape_success)
{
    struct RoiPoolingCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "ROIPooling",
        {
            {{{2, 4, 8, 8}, {2, 4, 8, 8}}, ge::DT_FLOAT, ge::FORMAT_ND}, // x [N, C, H, W]
            {{{10, 5}, {10, 5}}, ge::DT_FLOAT, ge::FORMAT_ND},           // rois [K, 5]
            {{{2}, {2}}, ge::DT_INT32, ge::FORMAT_ND},                   // roi_actual_num [N]
        },
        {
            {{{10, 4, 7, 7}, {10, 4, 7, 7}}, ge::DT_FLOAT, ge::FORMAT_ND}, // y [K, C, pooled_h, pooled_w]
        },
        {
            gert::TilingContextPara::OpAttr("pooled_h", Ops::NN::AnyValue::CreateFrom<int64_t>(7)),
            gert::TilingContextPara::OpAttr("pooled_w", Ops::NN::AnyValue::CreateFrom<int64_t>(7)),
            gert::TilingContextPara::OpAttr("spatial_scale_h", Ops::NN::AnyValue::CreateFrom<float>(1.0f)),
            gert::TilingContextPara::OpAttr("spatial_scale_w", Ops::NN::AnyValue::CreateFrom<float>(1.0f)),
        },
        &compileInfo, 32, 262144, 4096);

    TilingInfo tilingInfo;
    EXPECT_TRUE(ExecuteTiling(tilingContextPara, tilingInfo));

    EXPECT_EQ(tilingInfo.tilingKey, 0);

    auto* tiling = reinterpret_cast<const RoiPoolingTilingData*>(tilingInfo.tilingData.get());
    int64_t expectedTotalElements = 10 * 4 * 7 * 7;
    EXPECT_EQ(tiling->totalElements, expectedTotalElements);
    EXPECT_EQ(tiling->needCoreNum, 2);
    EXPECT_EQ(tiling->N, 2);
    EXPECT_EQ(tiling->K, 10);
    EXPECT_EQ(tiling->C, 4);
    EXPECT_EQ(tiling->H, 8);
    EXPECT_EQ(tiling->W, 8);
    EXPECT_EQ(tiling->pooledH, 7);
    EXPECT_EQ(tiling->pooledW, 7);
    EXPECT_FLOAT_EQ(tiling->spatialScaleH, 1.0f);
    EXPECT_FLOAT_EQ(tiling->spatialScaleW, 1.0f);
}

TEST_F(RoiPoolingTiling, roi_pooling_float16_medium_shape_success)
{
    struct RoiPoolingCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "ROIPooling",
        {
            {{{1, 3, 16, 16}, {1, 3, 16, 16}}, ge::DT_FLOAT16, ge::FORMAT_ND},
            {{{100, 5}, {100, 5}}, ge::DT_FLOAT16, ge::FORMAT_ND},
            {{{1}, {1}}, ge::DT_INT32, ge::FORMAT_ND},
        },
        {
            {{{100, 3, 6, 6}, {100, 3, 6, 6}}, ge::DT_FLOAT16, ge::FORMAT_ND},
        },
        {
            gert::TilingContextPara::OpAttr("pooled_h", Ops::NN::AnyValue::CreateFrom<int64_t>(6)),
            gert::TilingContextPara::OpAttr("pooled_w", Ops::NN::AnyValue::CreateFrom<int64_t>(6)),
            gert::TilingContextPara::OpAttr("spatial_scale_h", Ops::NN::AnyValue::CreateFrom<float>(0.5f)),
            gert::TilingContextPara::OpAttr("spatial_scale_w", Ops::NN::AnyValue::CreateFrom<float>(0.5f)),
        },
        &compileInfo, 32, 262144, 4096);

    TilingInfo tilingInfo;
    EXPECT_TRUE(ExecuteTiling(tilingContextPara, tilingInfo));

    EXPECT_EQ(tilingInfo.tilingKey, 0);

    auto* tiling = reinterpret_cast<const RoiPoolingTilingData*>(tilingInfo.tilingData.get());
    int64_t expectedTotalElements = 100 * 3 * 6 * 6;
    EXPECT_EQ(tiling->totalElements, expectedTotalElements);
    EXPECT_EQ(tiling->needCoreNum, 11);
    EXPECT_EQ(tiling->N, 1);
    EXPECT_EQ(tiling->K, 100);
    EXPECT_EQ(tiling->C, 3);
    EXPECT_EQ(tiling->H, 16);
    EXPECT_EQ(tiling->W, 16);
    EXPECT_EQ(tiling->pooledH, 6);
    EXPECT_EQ(tiling->pooledW, 6);
    EXPECT_FLOAT_EQ(tiling->spatialScaleH, 0.5f);
    EXPECT_FLOAT_EQ(tiling->spatialScaleW, 0.5f);
}

TEST_F(RoiPoolingTiling, roi_pooling_float32_large_shape_full_core_success)
{
    struct RoiPoolingCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "ROIPooling",
        {
            {{{4, 64, 32, 32}, {4, 64, 32, 32}}, ge::DT_FLOAT, ge::FORMAT_ND},
            {{{1000, 5}, {1000, 5}}, ge::DT_FLOAT, ge::FORMAT_ND},
            {{{4}, {4}}, ge::DT_INT32, ge::FORMAT_ND},
        },
        {
            {{{1000, 64, 7, 7}, {1000, 64, 7, 7}}, ge::DT_FLOAT, ge::FORMAT_ND},
        },
        {
            gert::TilingContextPara::OpAttr("pooled_h", Ops::NN::AnyValue::CreateFrom<int64_t>(7)),
            gert::TilingContextPara::OpAttr("pooled_w", Ops::NN::AnyValue::CreateFrom<int64_t>(7)),
            gert::TilingContextPara::OpAttr("spatial_scale_h", Ops::NN::AnyValue::CreateFrom<float>(0.25f)),
            gert::TilingContextPara::OpAttr("spatial_scale_w", Ops::NN::AnyValue::CreateFrom<float>(0.25f)),
        },
        &compileInfo, 32, 262144, 4096);

    TilingInfo tilingInfo;
    EXPECT_TRUE(ExecuteTiling(tilingContextPara, tilingInfo));

    EXPECT_EQ(tilingInfo.tilingKey, 0);

    auto* tiling = reinterpret_cast<const RoiPoolingTilingData*>(tilingInfo.tilingData.get());
    int64_t expectedTotalElements = 1000 * 64 * 7 * 7;
    EXPECT_EQ(tiling->totalElements, expectedTotalElements);
    EXPECT_EQ(tiling->needCoreNum, 32);
    EXPECT_EQ(tiling->N, 4);
    EXPECT_EQ(tiling->K, 1000);
    EXPECT_EQ(tiling->C, 64);
    EXPECT_EQ(tiling->H, 32);
    EXPECT_EQ(tiling->W, 32);
    EXPECT_EQ(tiling->pooledH, 7);
    EXPECT_EQ(tiling->pooledW, 7);
    EXPECT_FLOAT_EQ(tiling->spatialScaleH, 0.25f);
    EXPECT_FLOAT_EQ(tiling->spatialScaleW, 0.25f);
}
