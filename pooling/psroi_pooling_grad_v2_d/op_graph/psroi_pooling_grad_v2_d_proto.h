/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file psroi_pooling_grad_v2_d_proto.h
 * \brief PSROIPoolingGradV2D operator proto definition.
 */
#ifndef OPS_NN_POOLING_PSROI_POOLING_GRAD_V2_D_PROTO_H_
#define OPS_NN_POOLING_PSROI_POOLING_GRAD_V2_D_PROTO_H_

#include "graph/operator_reg.h"
#include "graph/operator.h"

namespace ge {

/**
 * @brief Performs the backward computation of position-sensitive ROI pooling.
 *
 * @par Inputs:
 * @li x: Upstream gradients with logical shape [B*R, O, G, G], where O=output_dim and G=group_size.
 * The public interface declares float16 and float32; the implementations below support float32 only.
 * @li rois: An ND Tensor of shape [B, 5, R], whose five planes contain batch_id, x1, y1, x2, and y2.
 * batch_id must be integral and in [0, B); coordinates must be finite and non-negative. Its dtype must match x.
 *
 * @par Outputs:
 * @li y: Feature-map gradients with logical shape [B, O*G*G, H, W] and the same dtype as x.
 *
 * @par Attributes:
 * @li spatial_scale: A required Float scaling ROI coordinates into feature-map coordinates; finite and positive.
 * @li output_dim: A required positive Int specifying the number O of pooled output channels.
 * @li group_size: A required Int specifying the position-sensitive spatial group size G, in [1, 127].
 * @li input_size: A required ListInt containing exactly [H, W], both positive.
 *
 * @par Product constraints:
 * @li Ascend 950PR/950DT: this arch35 implementation uses float32, with x and y both ND or both NCHW,
 * and rois always ND. Both x/y formats use the contiguous 4D shapes above. The GE graph example uses
 * NCHW for x/y to satisfy the existing graph verifier; the original ND computation entry remains supported.
 * This does not imply that the existing verifier accepts ND graphs. R=0 produces zero gradients.
 * Overlapping contributions are accumulated deterministically.
 * @li Atlas A2/A3 training/inference products: the existing CANN TBE implementation uses float32 NC1HWC0
 * for x and y (NCHW origin format), and float32 ND for rois. Physical shapes are
 * x=[B*R, ceil(O/16), G, G, 16] and y=[B, ceil(O*G*G/16), H, W, 16]; R must be a positive multiple of 16.
 * This is a separate implementation, not an arch35 kernel supported on A2/A3.
 * @note The public float16 declaration does not imply float16 kernel support. See README for product-specific
 * restrictions and the Ascend950-only GE graph example.
 */
#ifndef OPS_PROTO_DEF_PSROIPOOLINGGRADV2D
#define OPS_PROTO_DEF_PSROIPOOLINGGRADV2D
REG_OP(PSROIPoolingGradV2D)
    .INPUT(x, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(rois, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(y, TensorType({DT_FLOAT16, DT_FLOAT}))
    .REQUIRED_ATTR(spatial_scale, Float)
    .REQUIRED_ATTR(output_dim, Int)
    .REQUIRED_ATTR(group_size, Int)
    .REQUIRED_ATTR(input_size, ListInt)
    .OP_END_FACTORY_REG(PSROIPoolingGradV2D)
#endif

} // namespace ge

#endif // OPS_NN_POOLING_PSROI_POOLING_GRAD_V2_D_PROTO_H_
