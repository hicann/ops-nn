/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include "psroi_pooling_grad_v2_d_simt.h"

template <uint32_t indexMode>
__global__ __aicore__ void psroi_pooling_grad_v2_d(GM_ADDR x, GM_ADDR rois, GM_ADDR y, GM_ADDR workspace,
                                                   GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(PSROIPoolingGradV2DTilingData);
    GET_TILING_DATA_WITH_STRUCT(PSROIPoolingGradV2DTilingData, tilingData, tiling);
    if constexpr (indexMode == MATERIALIZE_MODE_32) {
        NsPSROIPoolingGradV2D::ProcessMaterialize<DTYPE_X, DTYPE_ROIS, DTYPE_Y, uint32_t>(
            x, rois, y, AscendC::GetUserWorkspace(workspace), &tilingData);
    } else if constexpr (indexMode == MATERIALIZE_MODE_64) {
        NsPSROIPoolingGradV2D::ProcessMaterialize<DTYPE_X, DTYPE_ROIS, DTYPE_Y, uint64_t>(
            x, rois, y, AscendC::GetUserWorkspace(workspace), &tilingData);
    } else if constexpr (indexMode == PRECOMPUTE_MODE_32) {
        NsPSROIPoolingGradV2D::ProcessPrecompute<DTYPE_X, DTYPE_ROIS, DTYPE_Y, uint32_t>(
            x, rois, y, AscendC::GetUserWorkspace(workspace), &tilingData);
    } else if constexpr (indexMode == PRECOMPUTE_MODE_64) {
        NsPSROIPoolingGradV2D::ProcessPrecompute<DTYPE_X, DTYPE_ROIS, DTYPE_Y, uint64_t>(
            x, rois, y, AscendC::GetUserWorkspace(workspace), &tilingData);
    } else if constexpr (indexMode == INDEX_MODE_32) {
        NsPSROIPoolingGradV2D::Process<DTYPE_X, DTYPE_ROIS, DTYPE_Y, uint32_t>(x, rois, y, &tilingData);
    } else {
        NsPSROIPoolingGradV2D::Process<DTYPE_X, DTYPE_ROIS, DTYPE_Y, uint64_t>(x, rois, y, &tilingData);
    }
}
