# Operator APIs (aclnn)

To make it easier to call operators, a set of C-based APIs (with the **aclnn** prefix) are provided. You do not need to provide the Intermediate Representation (IR) definition to facilitate efficient model and application development. This method is called single-operator API calling, or aclnn calling for short.

> **Deterministic Computing:**
>
> - Configuration: Due to different CANN or NPU models, multiple runs of the same operator may have different results. Under the same conditions (platform, device, version, and other random parameters), deterministic computing can be enabled for some operator APIs by using `aclrtCtxSetSysParamOpt` (see [AscendCL API (C)](https://www.hiascend.com/document/detail/zh/canncommercial/850/API/appdevgapi/aclcppdevg_03_0004.html)). This ensures that the results of multiple runs are the same.
> - Performance: An operator using deterministic computing is usually slower than using non-deterministic computing. Therefore, the performance of a single model run may deteriorate. However, in scenarios such as experiments and debugging, where the same results must be obtained upon multiple runs for fault locating, deterministic computing can improve efficiency.
> - Threads: The deterministic status can be set only once in the same thread. If the deterministic status is set multiple times, the last valid setting is used. A valid setting refers to a deterministic status that has been used to deliver an operator task. If no operator task is delivered, only the deterministic variable is enabled but it is not delivered to the operator. As a result, the operator is not executed.
>   Solution: Currently, it is not recommended that the deterministic state be set for a thread for multiple times. This problem exists no matter whether the binary function is enabled or not. It will be solved in later versions.

The following table lists the operator APIs.

|    API Name  |      Description    |    Deterministic Description (A2/A3)   |
|-----------|------------|------------|
| [aclnnAdaLayerNorm](../../norm/ada_layer_norm/docs/aclnnAdaLayerNorm_en.md)|Fuses LayerNorm with downstream Add and Mul, and adjusts the normalization process through the adaptive parameters scale and shift.|Defaults to a deterministic implementation.|
| [aclnnAdaLayerNormQuant](../../norm/ada_layer_norm_quant/docs/aclnnAdaLayerNormQuant_en.md)|Fuses AdaLayerNorm with downstream quantization (only DynamicQuant is supported currently). This operator is used to perform quantization operations on adaptive layer normalization, that is, to normalize the input data and quantize the data into low-precision integers to improve the compute efficiency and reduce the memory usage.|Defaults to a deterministic implementation.|
| [aclnnAdaLayerNormV2](../../norm/ada_layer_norm_v2/docs/aclnnAdaLayerNormV2_en.md)|Fuses LayerNorm with downstream Add and Mul, and adjusts the normalization process through the adaptive parameters scale and shift. Compared with the AdaLayerNorm operator, this operator has two additional parameters (the mean value of the input and the reciprocal of the standard deviation of the input), and provides additional restrictions on data types supported by weight and bias.|Defaults to a deterministic implementation.|
| [aclnnAdaptiveAvgPool2d](../../pooling/adaptive_avg_pool3d/docs/aclnnAdaptiveAvgPool2d_en.md)|Performs 2D adaptive average pooling on tensor self with the specified two-dimensional output shape (outputSize).|Defaults to a deterministic implementation.|
| [aclnnAdaptiveAvgPool2dBackward](../../pooling/adaptive_avg_pool3d_grad/docs/aclnnAdaptiveAvgPool2dBackward_en.md)|Performs backpropagation of [aclnnAdaptiveAvgPool2d](../../pooling/adaptive_avg_pool3d/docs/aclnnAdaptiveAvgPool2d_en.md).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAdaptiveAvgPool3d](../../pooling/adaptive_avg_pool3d/docs/aclnnAdaptiveAvgPool3d_en.md)|Performs 3D adaptive average pooling on tensor self with the specified three-dimensional output shape (outputSize).|Defaults to a deterministic implementation.|
| [aclnnAdaptiveAvgPool3dBackward](../../pooling/adaptive_avg_pool3d/docs/aclnnAdaptiveAvgPool3d_en.md)|Performs backpropagation of [aclnnAdaptiveAvgPool3d](../../pooling/adaptive_avg_pool3d/docs/aclnnAdaptiveAvgPool3d_en.md).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAdaptiveMaxPool2d](../../pooling/adaptive_max_pool3d/docs/aclnnAdaptiveMaxPool2d_en.md)|Computes the size of each kernel based on the input outputSize, and performs two-dimensional max pooling on the input self.|Defaults to a deterministic implementation.|
| [aclnnAdaptiveMaxPool2dBackward](../../pooling/adaptive_avg_pool3d_grad/docs/aclnnAdaptiveAvgPool2dBackward_en.md)|Performs backpropagation of [aclnnAdaptiveMaxPool2d](../../pooling/adaptive_max_pool3d/docs/aclnnAdaptiveMaxPool2d_en.md). The gradient is backfilled to the coordinate of the maximum value in each adaptive window. The coordinates are accumulated.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAdaptiveMaxPool3d](../../pooling/adaptive_max_pool3d/docs/aclnnAdaptiveMaxPool3d_en.md)|Computes the size of each kernel based on the input outputSize, and performs three-dimensional max pooling on the input self.|Defaults to a deterministic implementation.|
| [aclnnAdaptiveMaxPool3dBackward](../../pooling/adaptive_max_pool3d_grad/docs/aclnnAdaptiveMaxPool3dBackward_en.md)|Performs backpropagation of [aclnnAdaptiveMaxPool3d](../../pooling/adaptive_max_pool3d/docs/aclnnAdaptiveMaxPool3d_en.md). The gradient is backfilled to the coordinate of the maximum value in each adaptive window. The coordinates are accumulated.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAddbmm&aclnnInplaceAddbmm](../../matmul/batch_mat_mul_v3/docs/aclnnAddbmm&aclnnInplaceAddbmm_en.md) |Description: Multiplies batch1 and batch3 and sums up the results by the first dimension (batch dimension). Compresses the three-dimensional vector into a two-dimensional vector (the shape is that of the last two dimensions). Multiplies the result by α, and then adds the result to the product of β and self to obtain the result.|Defaults to a deterministic implementation.|Defaults to a deterministic implementation.|
| |Computes the sum of the product of α multiplied by mat1 and mat2 and the product of β multiplied by self.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAddmmWeightNz](../../matmul/mat_mul_v3/docs/aclnnAddmmWeightNz_en.md)|Computes the sum of the product of α multiplied by mat1 and mat2 and the product of β multiplied by self. Compared with the original addmm API, the new API supports the NZ format for mat2.|Defaults to a deterministic implementation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAddmv](../../matmul/addmv/docs/aclnnAddmv_en.md) | Performs matrix multiplication and then vector addition.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAddLayerNorm](../../norm/add_layer_norm/docs/aclnnAddLayerNorm_en.md)|Provides the AddLayerNorm function. |Defaults to a deterministic implementation.|
| [aclnnAddLayerNormGrad](../../norm/add_layer_norm_grad/docs/aclnnAddLayerNormGrad_en.md)|Normalizes the input data at the network layer to the [0, 1].|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| aclnnAddLayerNormQuant |The LayerNorm operator is a normalization operation commonly used in foundation models.|Defaults to a deterministic implementation.|
| [aclnnAddRelu&aclnnInplaceAddRelu](../../activation/relu/docs/aclnnAddRelu&aclnnInplaceAddRelu_en.md)|Performs the addition operation and activates the result.|Defaults to a deterministic implementation.|
| [aclnnAddRmsNormCast](../../norm/add_rms_norm_cast/docs/aclnnAddRmsNormCast_en.md)|The RmsNorm operator is a normalization operation commonly used in foundation models. The AddRmsNormCast operator fuses the Cast operator after AddRmsNorm to reduce move-in and move-out operations.|Defaults to a deterministic implementation.|
| [aclnnAddRmsNormDynamicQuant](../../norm/add_rms_norm_dynamic_quant/docs/aclnnAddRmsNormDynamicQuant_en.md)|Fuses the Add operator before RmsNorm and the normalized output of RmsNorm to one or two DynamicQuant operators to reduce move-in and move-out operations.|Defaults to a deterministic implementation.|
| [aclnnAddRmsNormDynamicQuantV2](../../norm/add_rms_norm_dynamic_quant/docs/aclnnAddRmsNormDynamicQuantV2_en.md)|The RmsNorm operator is a normalization operation commonly used in foundation models. Compared with the LayerNorm operator, the RmsNorm operator does not subtract the average value.|Defaults to a deterministic implementation.|
| [aclnnAddRmsNorm](../../norm/add_rms_norm/docs/aclnnAddRmsNorm_en.md)|The RmsNorm operator is a normalization operation commonly used in foundation models. Compared with the LayerNorm operator, the RmsNorm operator does not subtract the average value.|Defaults to a deterministic implementation.|
| [aclnnAddRmsNormQuant](../../norm/add_rms_norm_quant/docs/aclnnAddRmsNormQuant_en.md)|Fuses the Add operator before RmsNorm and the Quantize operator after RmsNorm to reduce move-in and move-out operations.|Defaults to a deterministic implementation.|
| [aclnnAddRmsNormQuantV2](../../norm/add_rms_norm_quant_v2/docs/aclnnAddRmsNormQuantV2_en.md)|The RmsNorm operator is a common standardization operation for foundation models. Compared with the LayerNorm operator, the RmsNorm operator removes the part of subtracting the mean value.|Defaults to a deterministic implementation.|
| [aclnnAdvanceStep](../../optim/advance_step/docs/aclnnAdvanceStep_en.md)|Advances the inference step, that is, updates the model status and generate new inputTokens, inputPositions, seqLens, and slotMapping in each generation step, improving the inference efficiency of vLLM.|Defaults to a deterministic implementation.|
| [aclnnAdvanceStepV2](../../optim/advance_step/docs/aclnnAdvanceStepV2_en.md)|Advances the inference step, that is, updates the model status and generate new inputTokens, inputPositions, seqLens, and slotMapping in each generation step, improving the inference efficiency of vLLM.|Defaults to a deterministic implementation.|
| [aclnnApplyAdamW](../../optim/apply_adam_w/docs/aclnnApplyAdamW_en.md)|Implements the AdamW optimizer function.|Defaults to a deterministic implementation.|
| [aclnnApplyAdamWQuant](../../optim/apply_adam_w_quant/docs/aclnnApplyAdamWQuant_en.md)|Uses the m and v inputs of the optimizer as indexes to obtain their values in the qmap, and multiplied the values by the absmax corresponding to each blockSize for dequantization. Then, the AdamW optimizer is implemented. The maximum value of the updated m and v is selected from each blockSize. Each blockSize of m and v corresponds to an absmax, and normalization is performed once. The indexes in the qmap corresponding to m and v are found using the binary search method as the output. The absmax is also used as the input for the next round of quantization.|Defaults to a deterministic implementation.|
| [aclnnApplyAdamWV2](../../optim/apply_adam_w_v2/docs/aclnnApplyAdamWV2_en.md)|Implements the AdamW optimizer function.|Defaults to a deterministic implementation.|
| [aclnnApplyFusedEmaAdam](../../optim/apply_fused_ema_adam/docs/aclnnApplyFusedEmaAdam_en.md)|Implements the FusedEmaAdam fusion optimizer.|Defaults to a deterministic implementation.|
| [aclnnApplyTopKTopP](../../index/apply_top_k_top_p_with_sorted/docs/aclnnApplyTopKTopP_en.md) |Performs top-k and top-p sampling and filtering on the original input logits. |Defaults to a deterministic implementation.|
| [aclnnAscendAntiQuant](../../quant/ascend_anti_quant_v2/docs/aclnnAscendAntiQuant_en.md)|Dequantizes the input x based on the input scale and offset.|Defaults to a deterministic implementation.|
| [aclnnAscendQuant](../../quant/ascend_quant_v2/docs/aclnnAscendQuant_en.md)|Quantizes the input x based on the input scale and offset. The sizes of scale and offset must be equal to the last dimension of x or equal to 1.|Defaults to a deterministic implementation.|
| [aclnnAscendQuantV3](../../quant/ascend_quant_v2/docs/aclnnAscendQuantV3_en.md)|Quantizes the input x. The axes of scale and offset can be set by using axis. The shapes of scale and offset must be the same as axes of x specified by axis or equal to 1.|Defaults to a deterministic implementation.|
| [aclnnAvgPool2d](../../pooling/avg_pool3_d/docs/aclnnAvgPool2d_en.md)|Performs two-dimensional average pooling over the input tensor with the window of $kH * kW$ and stride of $sH * sW$.|Defaults to a deterministic implementation.|
| [aclnnAvgPool2dBackward](../../pooling/avg_pool3_d_grad/docs/aclnnAvgPool2dBackward_en.md)|Performs backpropagation of [aclnnAvgPool2d](../../pooling/avg_pool3_d/docs/aclnnAvgPool2d_en.md), and calculates the input gradient of two-dimensional average pooling forward propagation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnAvgPool3d](../../pooling/avg_pool3_d/docs/aclnnAvgPool3d_en.md)|Performs three-dimensional average pooling over the input tensor with the window of $kD * kH * kW$ and stride of $sD * sH * sW$.|Defaults to a deterministic implementation.|
| [aclnnAvgPool3dBackward](../../pooling/avg_pool3_d_grad/docs/aclnnAvgPool3dBackward_en.md)|Performs backpropagation of [aclnnAvgPool3d](../../pooling/avg_pool3_d/docs/aclnnAvgPool3d_en.md), and calculates the input gradient of three-dimensional average pooling forward propagation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBaddbmm&aclnnInplaceBaddbmm](../../matmul/batch_mat_mul_v3/docs/aclnnBaddbmm&aclnnInplaceBaddbmm_en.md) |Computes the product of α and the matrix multiplication result of batch1 and batch2, and then sums the product with the product of β and self.|Defaults to a deterministic implementation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBatchMatMul](../../matmul/batch_mat_mul_v3/docs/aclnnBatchMatMul_en.md) |Performs matrix multiplication on tensors self and mat2.|Defaults to a deterministic implementation.|
| [aclnnBatchMatmulQuant](../../matmul/batch_matmul_quant/docs/aclnnBatchMatmulQuant_en.md)|Performs matrix multiplication with the input tensor whose dtype is float16 and the output tensor whose dtype is int8.|Defaults to a deterministic implementation.|
| [aclnnBatchMatMulWeightNz](../../matmul/batch_mat_mul_v3/docs/aclnnBatchMatMulWeightNz_en.md) |Performs matrix multiplication on the tensors self and mat2. mat2 supports only the AI processor affinity data layout format. self must be three-dimensional, and mat2 must be five-dimensional.|Defaults to a deterministic implementation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBatchNorm](../../norm/batch_norm_v3/docs/aclnnBatchNorm_en.md)|Regularizes a batch of data. The statistical result of the generated data after regularization is 0 (mean value) or 1 (standard deviation).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBatchNormElemt](../../norm/batch_norm_elemt/docs/aclnnBatchNormElemt_en.md)|Uses the global mean and standard deviation reciprocal as the operator input to perform batch normalization on x.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBatchNormElemtBackward](../../norm/sync_batch_norm_backward_elemt/docs/aclnnBatchNormElemtBackward_en.md)|Performs backpropagation of aclnnBatchNormElemt. It is used to compute the element-level gradient of the input tensor, so that the model parameters can be updated during backpropagation.|Defaults to a deterministic implementation.|
| [aclnnBatchNormBackward](../../norm/batch_norm_grad_v3/docs/aclnnBatchNormBackward_en.md)|Performs backpropagation of [aclnnBatchNorm](../../norm/batch_norm_v3/docs/aclnnBatchNorm_en.md). It is used to calculate the gradient of the input tensor so that the model parameters can be updated during backpropagation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBatchNormGatherStatsWithCounts](../../norm/sync_batch_norm_gather_stats_with_counts/docs/aclnnBatchNormGatherStatsWithCounts_en.md)|Collects the mean value and variance of data on all devices and updates the global mean value and the reciprocal of the standard deviation.|Defaults to a deterministic implementation.|
| [aclnnBatchNormReduce](../../norm/bn_training_reduce/docs/aclnnBatchNormReduce_en.md)|Performs the first step of regularization on data, which involves summing and squaring the data.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnBatchNormReduceBackward](../../norm/sync_batch_norm_backward_reduce/docs/aclnnBatchNormReduceBackward_en.md)|Computes the gradient of the BatchNorm operation during backpropagation and performs reduction operations on some intermediate results to optimize the compute efficiency.|Defaults to a deterministic implementation.|
| [aclnnBidirectionLSTM](../../rnn/bidirection_lstm/docs/aclnnBidirectionLSTM_en.md) | Computes the LSTM network, receives the input sequence and initial state, and returns the output sequence and final state.|Defaults to a deterministic implementation.|
| [aclnnBidirectionLSTMV2](../../rnn/bidirection_lstmv2/docs/aclnnBidirectionLSTMV2_en.md) | Computes the LSTM network, receives the input sequence and initial state, and returns the output sequence and final state.|Defaults to a deterministic implementation.|
| [aclnnBinaryCrossEntropy](../../loss/binary_cross_entropy/docs/aclnnBinaryCrossEntropy_en.md) | Computes the binary cross entropy (BCE) of self and target.|Defaults to a deterministic implementation.|
| [aclnnBinaryCrossEntropyBackward](../../loss/binary_cross_entropy_grad/docs/aclnnBinaryCrossEntropyBackward_en.md) | Computes the gradient value of backpropagation of the binary cross entropy.|Defaults to a deterministic implementation.|
| [aclnnBinaryCrossEntropyWithLogits](../../loss/binary_cross_entropy_grad/docs/aclnnBinaryCrossEntropyBackward_en.md) |Computes the BCELoss between the input logits and the target.|Defaults to a deterministic implementation.|
| [aclnnBinaryCrossEntropyWithLogitsBackward](../../loss/sigmoid_cross_entropy_with_logits_grad_v2/docs/aclnnBinaryCrossEntropyWithLogitsBackward_en.md) |Computes logits computation of the input self, and performs backward propagation of BCELoss using the computed logits and label value target.|Defaults to a deterministic implementation.|
| [aclnnBinaryCrossEntropyWithLogitsTargetBackward](../../loss/sigmoid_cross_entropy_with_logits_grad_v2/docs/aclnnBinaryCrossEntropyWithLogitsTargetBackward_en.md) |Computes logits of the input self, and performs backpropagation of the BECLoss with respect to the target label value target.|Defaults to a deterministic implementation.|
| [aclnnCelu&aclnnInplaceCelu](../../activation/celu_v2/docs/aclnnCelu&aclnnInplaceCelu_en.md) |aclnnCelu applies the Continuously Differentiable Exponential Linear Unit (CELU) function for each element x in the input tensor self, and stores the obtained result in the output tensor out.|Defaults to a deterministic implementation.|
| [aclnnChamferDistanceBackward](../../loss/chamfer_distance_grad/docs/aclnnChamferDistanceBackward_en.md) | Provides the backward operator of ChamferDistance, and computes the gradients of the forward inputs based on their contribution to the forward output and the initial gradients.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnClippedSwiglu](../../activation/clipped_swiglu/docs/aclnnClippedSwiglu_en.md) | Performs a clipped Swish Gated Linear Unit (SwiGlu) activation function, and implements the SwiGlu computation of x.|Defaults to a deterministic implementation.|
| [aclnnConvolution](../../conv/convolution_forward/docs/aclnnConvolution_en.md) |Implements convolution operations, supporting 1D, 2D, and 3D convolutions, as well as transposed convolution, dilated convolution, and grouped convolution.|Defaults to a deterministic implementation.|
| [aclnnConvolutionBackward](../../conv/convolution_backward/docs/aclnnConvolutionBackward_en.md) |Performs backpropagation of convolution.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnConvDepthwise2d](../../conv/convolution_forward/docs/aclnnConvDepthwise2d_en.md) |Implements the two-dimensional depthwise convolution operation (DepthwiseConv2D).|Defaults to a deterministic implementation.|
| [aclnnConvertWeightToINT4Pack](../../matmul/convert_weight_to_int4_pack/docs/aclnnConvertWeightToINT4Pack_en.md)|Preprocesses the input weight data to convert the layout of low-bit data from sparse storage to dense storage. When the [data format](../en/context/data_formats.md) of the output weightInt4Pack is set to FRACTAL_NZ, this operator converts the [data format](../en/context/data_formats.md) from ND to FRACTAL_NZ.|Defaults to a deterministic implementation.|
| [aclnnConvTbc](../../conv/convolution_forward/docs/aclnnConvTbc_en.md) |Implements one-dimensional temporal convolution (TBC).|Defaults to a deterministic implementation.|
| [aclnnConvTbcBackward](../../conv/convolution_backward/docs/aclnnConvTbcBackward_en.md) |Computes the backpropagation of temporal convolution.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnCrossEntropyLoss](../../loss/cross_entropy_loss/docs/aclnnCrossEntropyLoss_en.md) | Computes the cross-entropy loss of the input.|Defaults to a deterministic implementation.|
| [aclnnCrossEntropyLossGrad](../../loss/cross_entropy_loss_grad/docs/aclnnCrossEntropyLossGrad_en.md) | Performs backpropagation of aclnnCrossEntropyLoss.|Defaults to a deterministic implementation.|
| [aclnnCtcLoss](../../loss/ctc_loss_v2/docs/aclnnCtcLoss_en.md) | Computes the connectionist temporal classification loss.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnCtcLossBackward](../../loss/ctc_loss_v2_grad/docs/aclnnCtcLossBackward_en.md) | Computes the connectionist temporal classification (CTC) loss gradient for the backpropagation of aclnnCtcLoss.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnDeepNorm](../../norm/deep_norm/docs/aclnnDeepNorm_en.md)|Normalizes the elements of the input tensor x by computing the mean and standard deviation, producing an output tensor with zero mean and unit variance.|Defaults to a deterministic implementation.|
| [aclnnDeepNormGrad](../../norm/deep_norm_grad/docs/aclnnDeepNormGrad_en.md)|Performs backpropagation of [aclnnDeepNorm](../../norm/deep_norm/docs/aclnnDeepNorm_en.md), and calculates the gradients of tensors x, gx, and gamma, as well as the sum of tensor dy.|Defaults to a non-deterministic implementation. Enabling deterministic computing is not supported.|
| [aclnnDeformableConv2d](../../conv/deformable_conv2d/docs/aclnnDeformableConv2d_en.md)|Implements convolution operations, supporting 2D, deformable, and grouped convolutions.|Defaults to a deterministic implementation.|
| [aclnnDequantBias](../../quant/dequant_bias/docs/aclnnDequantBias_en.md)|Performs dequantization on input x, converting int32 data to FLOAT16 or BFLOAT16 for output.|Defaults to a deterministic implementation.|
| [aclnnDequantSwigluQuant](../../quant/dequant_swiglu_quant/docs/aclnnDequantSwigluQuant_en.md)|Adds dequantization and quantization operations before and after the Swish Gated Linear Unit (SwiGlu) activation function, performing DequantSwiGLUQuant computation on input x.|Defaults to a deterministic implementation.|
| aclnnDequantSwigluQuantV2 |Adds dequantization and quantization operations before and after the Swish Gated Linear Unit (SwiGlu) activation function, performing DequantSwiGLUQuant computation on input x.|Defaults to a deterministic implementation.|
| aclnnDynamicMxQuant|Performs dynamic symmetric quantization for the data of FLOAT4 and FLOAT8 types.|Defaults to a deterministic implementation.|
| [aclnnDynamicBlockQuant](../../quant/dynamic_block_quant/docs/aclnnDynamicBlockQuant_en.md)|Divides the input tensor into blocks according to the given rowBlockSize and colBlockSize, and performs quantization at the block level. For each block, the quantization parameter scaleOut is computed, and the input is quantized accordingly. The final quantized output and scaleOut for each block are returned.|Defaults to a deterministic implementation.|
| [aclnnDynamicQuant](../../quant/dynamic_quant_v2/docs/aclnnDynamicQuant_en.md)|Performs per-token symmetric dynamic quantization on input tensors.|Defaults to a deterministic implementation.|
| [aclnnDynamicQuantV2](../../quant/dynamic_quant_v2/docs/aclnnDynamicQuantV2_en.md)|Performs per-token symmetric/asymmetric dynamic quantization on the input tensor.|Defaults to a deterministic implementation.|
| aclnnDynamicQuantV3 |Performs dynamic quantization on the input tensor.|Defaults to a deterministic implementation.|
| [aclnnEinsum](../../matmul/batch_mat_mul_v3/docs/aclnnEinsum_en.md) |Performs tensor computation using the Einstein summation convention, following the pattern term1, term2 -> output-term. The output tensor is generated according to the following equation, where reduce-sum is applied to all indices that appear in the input terms (term1, term2) but not in the output term.|Defaults to a deterministic implementation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is not supported.|
| [aclnnElu&aclnnInplaceElu](../../activation/elu/docs/aclnnElu&aclnnInplaceElu_en.md) |Applies the Exponential Linear Unit (ELU) activation function to each element x in the input tensor self and stores the result in the output tensor out.|Defaults to a deterministic implementation.|
| [aclnnEluBackward](../../activation/elu_grad_v2/docs/aclnnEluBackward_en.md) |Performs backpropagation of the aclnnElu activation function and outputs the gradient of the forward input of the ELU activation function.|Defaults to a deterministic implementation.|
| [aclnnEmbedding](../../index/gather_v2/docs/aclnnEmbedding_en.md) |Maps a set of indices to a vector space and thereby quantizes the data. The two-dimensional weight tensor of the embedding operator is weight, which has m+1 rows and n columns. For any input index tensor indices (for example, 1 row and 3 columns), the output out is a tensor with 3 rows and n columns. |Defaults to a deterministic implementation.|
| [aclnnEmbeddingBag](../../index/embedding_bag/docs/aclnnEmbeddingBag_en.md) |Obtains a group of numbers from weight based on indices, and performs aggregation in max, sum, or mean mode on the obtained numbers based on offsets and the aggregation mode specified by mode. Other parameters provide finer control over the computation process. |Defaults to a deterministic implementation.|
| [aclnnEmbeddingDenseBackward](../../index/embedding_dense_grad_v2/docs/aclnnEmbeddingDenseBackward_en.md) |Performs backpropagation of aclnnEmbedding and accumulates rows of grad corresponding to identical indices entries into out. |Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnEmbeddingRenorm](../../index/gather_v2/docs/aclnnEmbeddingRenorm_en.md) |Returns the corrected result of the input tensor for the specified indices based on the given maxNorm and normType. |Defaults to a deterministic implementation.|
| [aclnnErfinv&aclnnInplaceErfinv](../../activation/erfinv/docs/aclnnErfinv&aclnnInplaceErfinv_en.md) |Returns the quantile of each element in the input tensor in the standard normal distribution function. erfinv is the inverse function of the Gaussian error function erf.|Defaults to a deterministic implementation.|
| [aclnnExpandIntoJaggedPermute](../../index/expand_into_jagged_permute/docs/aclnnExpandIntoJaggedPermute_en.md) |Extends the permutation indices of sparse data from the table dimension to the batch dimension, applicable to scenarios where sparse features have different batch sizes across ranks.|Defaults to a deterministic implementation.|
| [aclnnFakeQuantPerChannelAffineCachemask](../../quant/fake_quant_affine_cachemask/docs/aclnnFakeQuantPerChannelAffineCachemask_en.md)|Performs fake quantization on the input self along the specified axis with scale and zero_point, and updates the value range of the fake-quantized output based on quant_min and quant_max.|Defaults to a deterministic implementation.|
| [aclnnFakeQuantPerTensorAffineCachemask](../../quant/fake_quant_affine_cachemask/docs/aclnnFakeQuantPerTensorAffineCachemask_en.md)|Performs fake quantization on the input self, and updates the value range of the fake-quantized output based on quant_min and quant_max.|Defaults to a deterministic implementation.|
| [aclnnFastBatchNormBackward](../../norm/batch_norm_grad_v3/docs/aclnnFastBatchNormBackward_en.md) |Performs backpropagation of [aclnnBatchNorm](../../norm/batch_norm_v3/docs/aclnnBatchNorm_en.md) (high-performance version). It is used to calculate the gradient of the input tensor so that the model parameters can be updated during backpropagation.|Defaults to a deterministic implementation.|
| [aclnnFastGelu](../../activation/fast_gelu/docs/aclnnFastGelu_en.md) |Computes the fast Gaussian Error Linear Unit activation function.|Defaults to a deterministic implementation.|
| [aclnnFastGeluBackward](../../activation/fast_gelu_grad/docs/aclnnFastGeluBackward_en.md) |Performs backpropagation of FastGelu.|Defaults to a deterministic implementation.|
| [aclnnFatreluMul](../../activation/fatrelu_mul/docs/aclnnFatreluMul_en.md) |Divides the input tensor into two tensors, x1 and x2, along the last dimension, performs Threshold computation on x1, and multiplies the result by x2.|Defaults to a deterministic implementation.|
| [aclnnFlatQuant](../../quant/flat_quant/docs/aclnnFlatQuant_en.md)|Performs two small matrix multiplications on the input matrix x|Defaults to a deterministic implementation.|
| [aclnnFlip](../../index/reverse_v2/docs/aclnnFlip_en.md) | Reverses the order of the specified dimension of an n-dimensional tensor. There is a formula for calculating each axis specified in dims.|Defaults to a deterministic implementation.|
| [aclnnForeachAbs](../../foreach/foreach_abs/docs/aclnnForeachAbs_en.md) | Performs element-wise absolute value operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachAcos](../../foreach/foreach_acos/docs/aclnnForeachAcos_en.md) | Performs element-wise inverse cosine operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachAddcdivList](../../foreach/foreach_addcdiv_list/docs/aclnnForeachAddcdivList_en.md) | Performs element-wise addition, multiplication, and division on multiple tensors. It computes the element-wise division of $x2_{i}$ by $x3_{i}$, multiplies the result by scalars, and then adds the product to $x1_{i}$.|Defaults to a deterministic implementation.|
| [aclnnForeachAddcdivScalar](../../foreach/foreach_addcdiv_scalar/docs/aclnnForeachAddcdivScalar_en.md) | Performs element-wise addition, multiplication, and division on multiple tensors. It computes the element-wise division of $x2_{i}$ by $x3_{i}$, multiplies the result by scalar, and then adds the product to $x1_{i}$.|Defaults to a deterministic implementation.|
| [aclnnForeachAddcdivScalarList](../../foreach/foreach_addcdiv_scalar_list/docs/aclnnForeachAddcdivScalarList_en.md) | Performs element-wise addition, multiplication, and division on multiple tensors. It computes the element-wise division of $x2_{i}$ by $x3_{i}$, multiplies the result by $scalars_{i}$, and then adds the product to $x1_{i}$.|Defaults to a deterministic implementation.|
| [aclnnForeachAddcdivScalarV2](../../foreach/foreach_addcdiv_scalar/docs/aclnnForeachAddcdivScalarV2_en.md) | Performs element-wise addition, multiplication, and division on multiple tensors. It computes the element-wise division of $x2_{i}$ by $x3_{i}$, multiplies the result by scalar, and then adds the product to $x1_{i}$.|Defaults to a deterministic implementation.|
| [aclnnForeachAddcmulList](../../foreach/foreach_addcmul_list/docs/aclnnForeachAddcmulList_en.md) | Performs element-wise multiplication on tensor lists x2 and x3, multiplies the result by the tensor scalars, and then adds the result element-wise to tensor list x1. |Defaults to a deterministic implementation.|
| [aclnnForeachAddcmulScalar](../../foreach/foreach_addcmul_scalar/docs/aclnnForeachAddcmulScalar_en.md) | Performs element-wise multiplication on tensor lists x2 and x3, multiplies the result by the tensor scalar, and then adds the result element-wise to tensor list x1.|Defaults to a deterministic implementation.|
| [aclnnForeachAddcmulScalarList](../../foreach/foreach_addcmul_scalar_list/docs/aclnnForeachAddcmulScalarList_en.md) | Performs element-wise multiplication on tensor lists x2 and x3, multiplies the result element-wise by the tensor scalars, and then adds the result element-wise to tensor list x1. |Defaults to a deterministic implementation.|
| [aclnnForeachAddcmulScalarV2](../../foreach/foreach_addcmul_scalar/docs/aclnnForeachAddcmulScalarV2_en.md) |  Performs element-wise multiplication on tensor lists x2 and x3, multiplies the result by the scalar scalar, and then adds the result element-wise to tensor list x1. |Defaults to a deterministic implementation.|
| [aclnnForeachAddList](../../foreach/foreach_add_list/docs/aclnnForeachAddList_en.md) | Performs element-wise addition on the two tensor lists. The addition coefficient can be adjusted by the alpha parameter.|Defaults to a deterministic implementation.|
| [aclnnForeachAddListV2](../../foreach/foreach_add_list/docs/aclnnForeachAddListV2_en.md) | Performs element-wise addition on the two tensor lists. The addition coefficient can be adjusted by the alpha parameter. |Defaults to a deterministic implementation.|
| [aclnnForeachAddScalar](../../foreach/foreach_add_scalar/docs/aclnnForeachAddScalar_en.md) | Adds the specified tensor value to each tensor in the tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachAddScalarList](../../foreach/foreach_add_scalar/docs/aclnnForeachAddScalar_en.md) | Performs element-wise addition on the input tensor list and input scalar list.|Defaults to a deterministic implementation.|
| [aclnnForeachAddScalarV2](../../foreach/foreach_add_scalar/docs/aclnnForeachAddScalarV2_en.md) | Adds the specified scalar value to each tensor in the tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachAsin](../../foreach/foreach_asin/docs/aclnnForeachAsin_en.md) | Performs element-wise arcsine operation. |Defaults to a deterministic implementation.|
| [aclnnForeachAtan](../../foreach/foreach_atan/docs/aclnnForeachAtan_en.md) | Performs element-wise arctangent operation. |Defaults to a deterministic implementation.|
| [aclnnForeachCopy](../../foreach/foreach_copy/docs/aclnnForeachCopy_en.md) | Copies the content of two tensor lists.|Defaults to a deterministic implementation.|
| [aclnnForeachCos](../../foreach/foreach_cos/docs/aclnnForeachCos_en.md) | Performs element-wise cosine operation.|Defaults to a deterministic implementation.|
| [aclnnForeachCosh](../../foreach/foreach_cosh/docs/aclnnForeachCosh_en.md) | Performs element-wise hyperbolic cosine operation.|Defaults to a deterministic implementation.|
| [aclnnForeachDivList](../../foreach/foreach_div_list/docs/aclnnForeachDivList_en.md) | Performs element-wise division on the tensor lists x1 and x2.|Defaults to a deterministic implementation.|
| [aclnnForeachDivScalar](../../foreach/foreach_div_scalar/docs/aclnnForeachDivScalar_en.md) | Divides each tensor in tensor list x by the tensor scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachDivScalarList](../../foreach/foreach_div_scalar_list/docs/aclnnForeachDivScalarList_en.md) | Performs element-wise division on the tensor list x and the scalar list scalars.|Defaults to a deterministic implementation.|
| [aclnnForeachDivScalarV2](../../foreach/foreach_div_scalar/docs/aclnnForeachDivScalarV2_en.md) | Divides each tensor in tensor list x by the scalar scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachErf](../../foreach/foreach_erf/docs/aclnnForeachErf_en.md) | Performs element-wise error function (also known as Gaussian error function) operation.|Defaults to a deterministic implementation.|
| [aclnnForeachErfc](../../foreach/foreach_erfc/docs/aclnnForeachErfc_en.md) | Performs element-wise computation of the complementary error function, defined as the integral from x to infinity.|Defaults to a deterministic implementation.|
| [aclnnForeachExp](../../foreach/foreach_exp/docs/aclnnForeachExp_en.md) | Performs element-wise exponential operation on the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachExpm1](../../foreach/foreach_expm1/docs/aclnnForeachExpm1_en.md) | Performs exponential operation on each tensor in the input tensor list and subtracts 1 from the result.|Defaults to a deterministic implementation.|
| [aclnnForeachLerpList](../../foreach/foreach_lerp_list/docs/aclnnForeachLerpList_en.md) | Performs element-wise linear interpolation on corresponding tensors in two tensor lists using weight as the interpolation coefficients.|Defaults to a deterministic implementation.|
| [aclnnForeachLerpScalar](../../foreach/foreach_lerp_scalar/docs/aclnnForeachLerpScalar_en.md) | Performs interpolation on the corresponding elements of two tensor lists, where the scalar weight is the interpolation coefficient.|Defaults to a deterministic implementation.|
| [aclnnForeachLog](../../foreach/foreach_log/docs/aclnnForeachLog_en.md) | Performs element-wise natural logarithm operation (ln(x)) on the tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachLog1p](../../foreach/foreach_log1p/docs/aclnnForeachLog1p_en.md) | Adds 1 to each element in the input tensor list, then performs the e-based logarithmic function on the elements.|Defaults to a deterministic implementation.|
| [aclnnForeachLog2](../../foreach/foreach_log2/docs/aclnnForeachLog2_en.md) | Performs the 2-based logarithmic function on each element in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachLog10](../../foreach/foreach_log10/docs/aclnnForeachLog10_en.md) | Performs the 10-based logarithmic function on each element in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachMaximumList](../../foreach/foreach_maximum_list/docs/aclnnForeachMaximumList_en.md) | Computes the element-wise maximum of each pair of corresponding tensors in tensor lists x1 and x2.|Defaults to a deterministic implementation.|
| [aclnnForeachMaximumScalar](../../foreach/foreach_maximum_scalar/docs/aclnnForeachMaximumScalar_en.md) | Computes the element-wise maximum of each tensor in the input tensor list with the given tensor scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachMaximumScalarList](../../foreach/foreach_maximum_scalar_list/docs/aclnnForeachMaximumScalarList_en.md) | Computes the element-wise maximum of each tensor in tensor list x and the corresponding scalar in scalar list scalars.|Defaults to a deterministic implementation.|
| [aclnnForeachMaximumScalarV2](../../foreach/foreach_maximum_scalar/docs/aclnnForeachMaximumScalarV2_en.md) | Computes the element-wise maximum of each tensor in the input tensor list with the given scalar scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachMinimumList](../../foreach/foreach_minimum_list/docs/aclnnForeachMinimumList_en.md) | Computes the element-wise minimum of each pair of corresponding tensors in tensor lists x1 and x2.|Defaults to a deterministic implementation.|
| [aclnnForeachMinimumScalar](../../foreach/foreach_minimum_scalar/docs/aclnnForeachMinimumScalar_en.md) | Computes the element-wise minimum of each tensor in tensor list x with the given tensor scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachMinimumScalarList](../../foreach/foreach_minimum_scalar_list/docs/aclnnForeachMinimumScalarList_en.md) | Computes the element-wise minimum of each tensor in tensor list x and the corresponding scalar in scalar list scalars.|Defaults to a deterministic implementation.|
| [aclnnForeachMinimumScalarV2](../../foreach/foreach_minimum_scalar/docs/aclnnForeachMinimumScalarV2_en.md) | Computes the element-wise minimum of each tensor in tensor list x with the given scalar scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachMulList](../../foreach/foreach_mul_list/docs/aclnnForeachMulList_en.md) | Performs element-wise multiplication on corresponding tensors in two input tensor lists.|Defaults to a deterministic implementation.|
| [aclnnForeachMulScalar](../../foreach/foreach_mul_scalar/docs/aclnnForeachMulScalar_en.md) | Performs element-wise multiplication between each tensor in the input tensor list and the given tensor scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachMulScalarList](../../foreach/foreach_mul_scalar_list/docs/aclnnForeachMulScalarList_en.md) | Performs element-wise multiplication of each tensor in the input tensor list by the corresponding scalar in the scalar list.|Defaults to a deterministic implementation.|
| [aclnnForeachMulScalarV2](../../foreach/foreach_mul_scalar/docs/aclnnForeachMulScalarV2_en.md) | Performs element-wise multiplication between each tensor in the input tensor list and the given tensor scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachNeg](../../foreach/foreach_neg/docs/aclnnForeachNeg_en.md) | Computes the negative of each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachNonFiniteCheckAndUnscale](../../foreach/foreach_non_finite_check_and_unscale/docs/aclnnForeachNonFiniteCheckAndUnscale_en.md) |  Iterates over all tensors in scaledGrads to check for Inf or NaN. Sets foundInf to 1.0 if any Inf or NaN is detected; otherwise, foundInf remains unchanged. Then applies inverse scaling to all tensors in scaledGrads.|Defaults to a deterministic implementation.|
| [aclnnForeachNorm](../../foreach/foreach_norm/docs/aclnnForeachNorm_en.md) | Computes the norm of each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachPowList](../../foreach/foreach_pow_list/docs/aclnnForeachPowList_en.md) | Raise each tensor in the input tensor list to the power of x2.|Defaults to a deterministic implementation.|
| [aclnnForeachPowScalar](../../foreach/foreach_pow_scalar/docs/aclnnForeachPowScalar_en.md) | Raise each tensor in the input tensor list to the power of n.|Defaults to a deterministic implementation.|
| [aclnnForeachPowScalarV2](../../foreach/foreach_pow_scalar/docs/aclnnForeachPowScalarV2_en.md) | Raise each tensor in the input tensor list to the power of n.|Defaults to a deterministic implementation.|
| [aclnnForeachPowScalarAndTensor](../../foreach/foreach_pow_scalar_and_tensor/docs/aclnnForeachPowScalarAndTensor_en.md) | Raise each tensor in the input tensor list to the power of x.|Defaults to a deterministic implementation.|
| [aclnnForeachPowScalarList](../../foreach/foreach_pow_scalar_list/docs/aclnnForeachPowScalarList_en.md) | Raise each tensor in the input tensor list to the power of n.|Defaults to a deterministic implementation.|
| [aclnnForeachReciprocal](../../foreach/foreach_reciprocal/docs/aclnnForeachReciprocal_en.md) | Compute the reciprocal of each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachRoundOffNumber](../../foreach/foreach_round_off_number/docs/aclnnForeachRoundOffNumber_en.md) | Rounds off each tensor in the input tensor list with a specified precision. The rounding mode can be specified using roundMode.|Defaults to a deterministic implementation.|
| [aclnnForeachRoundOffNumberV2](../../foreach/foreach_round_off_number/docs/aclnnForeachRoundOffNumberV2_en.md) | Rounds off each tensor in the input tensor list with a specified precision. The rounding mode can be specified using roundMode.|Defaults to a deterministic implementation.|
| [aclnnForeachSigmoid](../../foreach/foreach_sigmoid/docs/aclnnForeachSigmoid_en.md) | Performs the Sigmoid function operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachSign](../../foreach/foreach_sign/docs/aclnnForeachSign_en.md) | Computes the symbolic value of each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachSin](../../foreach/foreach_sinh/docs/aclnnForeachSinh_en.md) | Performs the sine function operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachSinh](../../foreach/foreach_sinh/docs/aclnnForeachSinh_en.md) | Performs the hyperbolic sine function operation on each tensor in the input tensor list. |Defaults to a deterministic implementation.|
| [aclnnForeachSqrt](../../foreach/foreach_sqrt/docs/aclnnForeachSqrt_en.md) | Performs the square root operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachSubList](../../foreach/foreach_sub_list/docs/aclnnForeachSubList_en.md) | Performs element-wise subtraction on two input tensor lists and adjusts the subtraction coefficient using the alpha parameter.|Defaults to a deterministic implementation.|
| [aclnnForeachSubListV2](../../foreach/foreach_sub_list/docs/aclnnForeachSubListV2_en.md) | Subtracts elements in two tensor lists one by one and adjusts the subtraction coefficient using the alpha parameter. |Defaults to a deterministic implementation.|
| [aclnnForeachSubScalar](../../foreach/foreach_sub_scalar/docs/aclnnForeachSubScalar_en.md) | Performs element-wise subtraction between each tensor in the input tensor list and the given tensor scalar.|Defaults to a deterministic implementation.|
| [aclnnForeachSubScalarList](../../foreach/foreach_sub_scalar_list/docs/aclnnForeachSubScalarList_en.md) | Performs element-wise subtraction between each tensor in the input tensor list and each scalar in scalar list scalars.|Defaults to a deterministic implementation.|
| [aclnnForeachSubScalarV2](../../foreach/foreach_sub_scalar/docs/aclnnForeachSubScalarV2_en.md) | Performs element-wise subtraction between each tensor in the input tensor list and the given scalar scalar. |Defaults to a deterministic implementation.|
| [aclnnForeachTan](../../foreach/foreach_tan/docs/aclnnForeachTan_en.md) | Performs the tangent function operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachTanh](../../foreach/foreach_tanh/docs/aclnnForeachTanh_en.md) | Performs the hyperbolic tangent function operation on each tensor in the input tensor list.|Defaults to a deterministic implementation.|
| [aclnnForeachZeroInplace](../../foreach/foreach_zero_inplace/docs/aclnnForeachZeroInplace_en.md) | Updates the input tensor list in place, and sets each tensor to 0. |Defaults to a deterministic implementation.|
| aclnnFusedMatmul |Fuses matrix multiplication and general vector computation.|Defaults to a deterministic implementation.| Defaults to a deterministic implementation.|
| [aclnnFusedLinearOnlineMaxSum](../../matmul/fused_linear_online_max_sum/docs/aclnnFusedLinearOnlineMaxSum_en.md) |The function is equivalent to the implementation of Megatron's matmul and fused\_vocab\_parallel\_cross\_entropy. It supports splitting and fusion of matmul and celoss in the vocabulary\_size dimension.|Defaults to a deterministic implementation.|
| [aclnnFusedCrossEntropyLossWithMaxSum](../../loss/fused_cross_entropy_loss_with_max_sum/docs/aclnnFusedCrossEntropyLossWithMaxSum_en.md) |Solves the video memory and computing efficiency problems in the case of ultra-large vocabulary. This part involves the result of computing loss and softMax. This operator is a part of the cross entropy computation module in the vocabulary parallelism scenario.|Defaults to a deterministic implementation.|
| [aclnnFusedLinearCrossEntropyLossGrad](../../matmul/fused_linear_cross_entropy_loss_grad/docs/aclnnFusedLinearCrossEntropyLossGrad_en.md) |Solves the video memory and computing efficiency problems in the case of ultra-large vocabulary. This part involves the gradient computation implementation, which is used to compute the gradients of leaf nodes input and weight. This operator is a part of the cross entropy computation module in the vocabulary parallelism scenario.|Defaults to a deterministic implementation.|
| [aclnnGather](../../index/gather_elements_v2/docs/aclnnGather_en.md) | Gathers data of a specific dimension dim in the input tensor. |Defaults to a deterministic implementation.|
| [aclnnGatherNd](../../index/gather_nd/docs/aclnnGatherNd_en.md) | For an input tensor self with dimension r≥1 and an input tensor indices with dimension q≥1, collects data slices to the output tensor out with dimension (q – 1) + (r – indices_shape[–1]). |Defaults to a deterministic implementation.|
| [aclnnGatherV2](../../index/gather_v2/docs/aclnnGatherV2_en.md) | Extracts elements from the specified dimension dim of the input tensor based on the index number in index, and saves the elements to the out tensor. |Defaults to a deterministic implementation.|
| [aclnnGatherV3](../../index/gather_v2/docs/aclnnGatherV3_en.md) | Extracts elements from the specified dimension dim of the input tensor based on the index number in index. batchDims indicates the batch size. The result is saved to the out tensor. |Defaults to a deterministic implementation.|
| [aclnnGeGlu](../../activation/ge_glu_v2/docs/aclnnGeGlu_en.md) |Uses the Gaussian Error Linear Unit (GELU) activation function.|Defaults to a deterministic implementation.|
| [aclnnGeGluBackward](../../activation/ge_glu_grad_v2/docs/aclnnGeGluBackward_en.md) |Performs backpropagation of aclnnGeGlu.|Defaults to a deterministic implementation.|
| [aclnnGeGluV3](../../activation/ge_glu_v2/docs/aclnnGeGluV3_en.md) |Uses the Gaussian Error Linear Unit (GELU) activation gate function. Based on aclnnGeGlu, the direction of the data block on which the activation function is performed can be set.|Defaults to a deterministic implementation.|
| [aclnnGeGluV3Backward](../../activation/ge_glu_grad_v2/docs/aclnnGeGluV3Backward_en.md) |Performs backpropagation of aclnnGeGluV3.|Defaults to a deterministic implementation.|
| [aclnnGelu](../../activation/gelu/docs/aclnnGelu_en.md) |Uses the Gaussian Error Linear Unit (GELU) activation function.|Defaults to a deterministic implementation.|
| [aclnnGeluBackward](../../activation/gelu_grad/docs/aclnnGeluBackward_en.md) |Performs backpropagation of aclnnGelu.|Defaults to a deterministic implementation.|
| [aclnnGeluBackwardV2](../../activation/gelu_grad_v2/docs/aclnnGeluBackwardV2_en.md) |Performs backpropagation of aclnnGeluV2.|Defaults to a deterministic implementation.|
| [aclnnGeluMul](../../activation/gelu_mul/docs/aclnnGeluMul_en.md) |Divides the input tensor into two tensors x1 and x2 based on the last dimension, performs Gelu computation on x1, and multiplies the computation result by x2.|Defaults to a deterministic implementation.|
| aclnnGeluQuant |Fuses GeluV2 with DynamicQuant/AscendQuantV2, performs gelu activation on the input data self, quantizes the activation result, and outputs the quantized result.|Defaults to a deterministic implementation.|
| [aclnnGeluV2](../../activation/gelu_v2/docs/aclnnGeluV2_en.md) |Uses the Gaussian Error Linear Unit (GELU) activation function.|Defaults to a deterministic implementation.|
| [aclnnGemm](../../matmul/gemm/docs/aclnnGemm_en.md) |Computes the product of α multiplied by A and B, and then sum up the product of alpha multiplied by β and input C.|Defaults to a deterministic implementation.|Defaults to a deterministic implementation.|
| [aclnnGemmaRmsNorm](../../norm/gemma_rms_norm/docs/aclnnGemmaRmsNorm_en.md)|The GemmaRmsNorm operator is a normalization operation commonly used in foundation models. Compared with the RmsNorm operator, it performs the +1 operation on gamma during computation.|Defaults to a deterministic implementation.|
| [aclnnGlu](../../activation/sigmoid/docs/aclnnGlu_en.md) |The Gated Linear Unit (GLU) function splits an input tensor evenly into two tensors along the specified dimension dim and multiplies the former tensor by the Sigmoid output of the latter tensor element-wise.|Defaults to a deterministic implementation.|
| [aclnnGluBackward](../../activation/sigmoid/docs/aclnnGluBackward_en.md) |Performs backpropagation of aclnnGlu.|Defaults to a deterministic implementation.|
| [aclnnHardshrink](../../activation/hard_shrink/docs/aclnnHardshrink_en.md) |Forcibly shrinks elements in the range of λ by element.|Defaults to a deterministic implementation.|
| [aclnnGroupNorm](../../norm/group_norm/docs/aclnnGroupNorm_en.md)|Computes the group normalization result out, mean value meanOut, and reciprocal of the standard deviation rstdOut of the input self.|Defaults to a deterministic implementation.|
| [aclnnGroupNormBackward](../../norm/group_norm_grad/docs/aclnnGroupNormBackward_en.md)|Performs backpropagation of [aclnnGroupNorm](../../norm/group_norm/docs/aclnnGroupNorm_en.md). It is used to calculate the gradient of the input tensor so that the model parameters can be updated during backpropagation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnGroupNormSiluV2](../../norm/group_norm_silu/docs/aclnnGroupNormSiluV2_en.md)|Computes the group normalization result out, meanOut, reciprocal rstdOut of the standard deviation, and silu output of the input self.|Defaults to a deterministic implementation.|
| [aclnnGroupNormSwish](../../norm/group_norm_swish/docs/aclnnGroupNormSwish_en.md)|Computes the group normalization result out, mean value meanOut, reciprocal rstdOut of the standard deviation, and swish output of the input x.|Defaults to a deterministic implementation.|
| [aclnnGroupNormSwishGrad](../../norm/group_norm_swish_grad/docs/aclnnGroupNormSwishGrad_en.md)|Performs backpropagation of [aclnnGroupNormSwish](../../norm/group_norm_swish/docs/aclnnGroupNormSwish_en.md).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnGroupQuant](../../quant/group_quant/docs/aclnnGroupQuant_en.md)|Performs group-wise quantization on the input x.|Defaults to a deterministic implementation.|
| [aclnnHardshrinkBackward](../../activation/hard_shrink_grad/docs/aclnnHardshrinkBackward_en.md) |Calculates the backpropagation gradient gradInput for aclnnHardshrink.|Defaults to a deterministic implementation.|
| [aclnnHardsigmoid&aclnnInplaceHardsigmoid](../../activation/hard_sigmoid/docs/aclnnHardsigmoid&aclnnInplaceHardsigmoid_en.md) |Performs an activation function that returns a new tensor based on the formula. The shape of the result is the same as that of the input tensor.|Defaults to a deterministic implementation.|
| [aclnnHardsigmoidBackward](../../activation/hard_sigmoid_grad/docs/aclnnHardsigmoidBackward_en.md) |Performs backpropagation of aclnnHardsigmoid.|Defaults to a deterministic implementation.|
| [aclnnHardswishBackward](../../activation/hard_swish_grad/docs/aclnnHardswishBackward_en.md) |Performs backpropagation of aclnnHardswish and computes the gradient of tensor self.|Defaults to a deterministic implementation.|
| [aclnnHardswish&aclnnInplaceHardswish](../../activation/hard_swish/docs/aclnnHardswish&aclnnInplaceHardswish_en.md) |Performs an activation function. The output tensor has the same shape as the input tensor. If the input value is less than –3, the output is 0. If the input value is greater than 3, the output is the value. Otherwise, the output is (value + 3) × value/6.|Defaults to a deterministic implementation.|
| [aclnnHardswishBackwardV2](../../activation/hard_swish_grad_v2/docs/aclnnHardswishBackwardV2_en.md) |Performs backpropagation of aclnnHardswish and computes the gradient of tensor self.|Defaults to a deterministic implementation.|
| [aclnnHardtanhBackward](../../activation/hardtanh_grad/docs/aclnnHardtanhBackward_en.md) |Performs backpropagation of activation function aclnnHardtanh.|Defaults to a deterministic implementation.|
| [aclnnHeaviside](../../activation/heaviside/docs/aclnnHeaviside_en.md) |Computes the step function Heaviside of each element in input as the activation function of the model.|Defaults to a deterministic implementation.|
| [aclnnIndex](../../index/index/docs/aclnnIndex_en.md) | Obtains the coordinate data of the input x based on indices. |Defaults to a deterministic implementation.|
| [aclnnIndexAdd](../../index/inplace_scatter_add/docs/aclnnIndexAdd_en.md) | Adds the value in the source tensor to the value of the corresponding position in the input tensor based on the given index in a specified dimension.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnIndexCopy&aclnnInplaceIndexCopy](../../index/scatter_update/docs/aclnnIndexCopy&aclnnInplaceIndexCopy_en.md) | Takes the element value in the index tensor as the index and copies elements from source to the corresponding positions in selfRef based on the specified axis dim.|Defaults to a deterministic implementation.|
| [aclnnIndexFill&aclnnInplaceIndexFill](../../index/index_fill_d/docs/aclnnIndexFill&aclnnInplaceIndexFill_en.md) | Replaces the value at the position specified by index with value along the specified axis dim of the input self. The computation result is deterministic. |Defaults to a deterministic implementation.|
| [aclnnIndexFillTensor&aclnnInplaceIndexFillTensor](../../index/index_fill_d/docs/aclnnIndexFillTensor&aclnnInplaceIndexFillTensor_en.md) | Replaces the value at the position specified by index with value along the specified axis dim of the input self.|Defaults to a deterministic implementation.|
| [aclnnIndexPutImpl](../../index/index_put_v2/docs/aclnnIndexPutImpl_en.md) | Replaces or accumulates the coordinate data of input x and the input value based on indices.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnInplacePut](../../index/scatter_nd_update/docs/aclnnInplacePut_en.md) | Takes selfRef as a one-dimensional tensor and uses the element value in the index tensor as the index. If accumulate is true, the elements in source and the elements in the corresponding positions of selfRef are accumulated. If accumulate is false, the elements in source replace the elements in the corresponding positions of selfRef.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnIndexSelect](../../index/gather_v2/docs/aclnnIndexSelect_en.md) | Extracts elements from the specified dimension dim of the input tensor based on the index number in index, and saves the elements to the out tensor.|Defaults to a deterministic implementation.|
| [aclnnInplaceAddRmsNorm](../../norm/inplace_add_rms_norm/docs/aclnnInplaceAddRmsNorm_en.md)|The RmsNorm operator is a normalization operation commonly used in foundation models. Compared with the LayerNorm operator, the RmsNorm operator does not subtract the average value.|Defaults to a deterministic implementation.|
| aclnnInstanceNorm |Performs instance normalization.|Defaults to a deterministic implementation.|
| [aclnnInverse](../../index/matrix_inverse/docs/aclnnInverse_en.md) | Obtains the inverse matrix of an array.|Defaults to a deterministic implementation.|
| [aclnnKlDivBackward](../../loss/kl_div_loss_grad/docs/aclnnKlDivBackward_en.md) | Performs backpropagation on the result of the aclnnKlDiv API.|Defaults to a deterministic implementation.|
| [aclnnKthvalue](../../index/gather_v2/docs/aclnnKthvalue_en.md) | Returns the k-th minimum value and index of the input tensor in the specified dimension.|Defaults to a deterministic implementation.|
| aclnnKvRmsNormRopeCache |Splits the tail axis of the input tensor (kv) into the left half for rms_norm computation and the right half for rope computation, and then scatters the computation results to two caches.|Defaults to a deterministic implementation.|
| [aclnnL1Loss](../../loss/lp_loss/docs/aclnnL1Loss_en.md) | Computes the mean absolute error (MAE) between each element of input self and target.|Defaults to a deterministic implementation.|
| [aclnnL1LossBackward](../../loss/l1_loss_grad/docs/aclnnL1LossBackward_en.md) | Performs backpropagation of aclnnL1Loss. reduction specifies the computation method of the loss function.|Defaults to a deterministic implementation.|
| [aclnnLayerNorm&aclnnLayerNormWithImplMode](../../norm/layer_norm_v4/docs/aclnnLayerNorm&aclnnLayerNormWithImplMode_en.md)|Performs normalization with a mean of 0 and a standard deviation of 1 on a specified layer.|Defaults to a deterministic implementation.|
| [aclnnLayerNormBackward](../../norm/layer_norm_grad_v3/docs/aclnnLayerNormBackward_en.md)|Performs backpropagation of [aclnnNorm](../../norm/lp_norm_v2/docs/aclnnNorm_en.md). It is used to calculate the gradient of the input tensor so that the model parameters can be updated during backpropagation.|Defaults to a deterministic implementation.|
| [aclnnLeakyRelu&aclnnInplaceLeakyRelu](../../activation/leaky_relu/docs/aclnnLeakyRelu&aclnnInplaceLeakyRelu_en.md) |Performs an activation function, which is used to solve the problem that the output of the Relu function is 0 when the input is less than 0 to prevent neurons from failing to update parameters.|Defaults to a deterministic implementation.|
| [aclnnLeakyReluBackward](../../activation/leaky_relu_grad/docs/aclnnLeakyReluBackward_en.md) |Performs backpropagation of the LeakyRelu activation function.|Defaults to a deterministic implementation.|
| [aclnnLinalgVectorNorm](../../norm/lp_norm_v2/docs/aclnnLinalgVectorNorm_en.md)|Computes the vector norm of the input tensor.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnLogit](../../loss/logit/docs/aclnnLogit_en.md) | This operator is a mathematical operation of converting probability to log-odds. It is often used for inverse transformation of probability values.|Defaults to a deterministic implementation.|
| [aclnnLogitGrad](../../loss/logit_grad/docs/aclnnLogitGrad_en.md) | Performs backpropagation of aclnnLogit.|Defaults to a deterministic implementation.|
| [aclnnLogSigmoid](../../activation/logsigmoid/docs/aclnnLogSigmoid_en.md) |Performs element-wise LogSigmoid operation on the input tensor.|Defaults to a deterministic implementation.|
| [aclnnLogSigmoidBackward](../../activation/logsigmoid_grad/docs/aclnnLogSigmoidBackward_en.md) |Performs backpropagation of aclnnLogSigmoid. It is used to compute the gradient input based on the gradient generated during upper-layer propagation and the forward input of LogSigmoid.|Defaults to a deterministic implementation.|
| [aclnnLogSigmoidForward](../../activation/logsigmoid/docs/aclnnLogSigmoidForward_en.md) |Performs element-wise LogSigmoid operation on the input tensor.|Defaults to a deterministic implementation.|
| [aclnnLogSoftmax](../../activation/logsoftmax_v2/docs/aclnnLogSoftmax_en.md) |Computes the logsoftmax value of the input tensor.|Defaults to a deterministic implementation.|
| [aclnnLogSoftmaxBackward](../../activation/logsoftmax_grad/docs/aclnnLogSoftmaxBackward_en.md) |Performs backpropagation of aclnnLogSoftmax.|Defaults to a deterministic implementation.|
| [aclnnMaskedSoftmaxWithRelPosBias](../../norm/masked_softmax_with_rel_pos_bias/docs/aclnnMaskedSoftmaxWithRelPosBias_en.md) |Replaces the part that uses window attention to compute softmax in swinTransformer.|Defaults to a deterministic implementation.|
| [aclnnMatmul](../../matmul/mat_mul_v3/docs/aclnnMatmul_en.md) |Performs matrix multiplication on the one-to-six-dimensional tensor self and the tensor mat2.|Defaults to a deterministic implementation.|
| aclnnMatmulCompress |During l@r matrix multiplication, the msModelSlim tool can be used to losslessly compress the r matrix to reduce the memory usage of the r matrix, and then this API is used to complete lossless decompression, matrix multiplication, and dequantization.|Defaults to a deterministic implementation.|
| aclnnCalculateMatmulWeightSize |Calculates the size of the weight to be allocated when the Matmul operator is in ND format. This API is used only to determine the size required for preprocessing the weight tensor to achieve optimal Matmul operator execution performance.|Defaults to a deterministic implementation.|
| aclnnCalculateMatmulWeightSizeV2 |Calculates the size of the space (in the number of elements) required for converting the Matmul operator from the ND format to the NZ format. This API is used only to determine the size required for preprocessing the weight tensor to optimize the execution performance of the Matmul operator.|Defaults to a deterministic implementation.|
| [aclnnInplaceMaskedScatter](../../index/masked_scatter/docs/aclnnInplaceMaskedScatter_en.md) |Copies the element in source to the corresponding position in selfRef based on the position where the element in the mask tensor is True.|Defaults to a deterministic implementation.|
| [aclnnMatmulWeightNz](../../matmul/mat_mul_v3/docs/aclnnMatmulWeightNz_en.md) |Performs matrix multiplication on tensors self and mat2. mat2 supports only the AI processor affinity data layout format.|Defaults to a deterministic implementation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMaxPool](../../pooling/max_pool_v3/docs/aclnnMaxPool_en.md)|Applies three-dimensional or four-dimensional max pooling over the input channels of an input signal, and outputs the value out after pooling.|Defaults to a deterministic implementation.|
| [aclnnMaxPool2dWithIndices](../../pooling/max_pool3d_with_argmax_v2/docs/aclnnMaxPool2dWithIndices_en.md)|Provides two-dimensional (H, W) max pooling for the input channels of the input signal, and outputs the pooled value out and indices.|Defaults to a deterministic implementation.|
| [aclnnMaxPool2dWithIndicesBackward](../../pooling/max_pool3d_grad_with_argmax/docs/aclnnMaxPool2dWithIndicesBackward_en.md)|Performs backpropagation of [aclnnMaxPool2dWithIndices](../../pooling/max_pool3d_with_argmax_v2/docs/aclnnMaxPool2dWithIndices_en.md).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMaxPool2dWithMask](../../pooling/max_pool3d_with_argmax_v2/docs/aclnnMaxPool2dWithMask_en.md)|Provides two-dimensional max pooling for the input channels of the input signal, and outputs the pooled value out and indices (calculated using the mask semantics).|Defaults to a deterministic implementation.|
| [aclnnMaxPool2dWithMaskBackward](../../pooling/max_pool3d_grad_with_argmax/docs/aclnnMaxPool2dWithMaskBackward_en.md)|Performs backpropagation of [aclnnMaxPool2dWithMask](../../pooling/max_pool3d_with_argmax_v2/docs/aclnnMaxPool2dWithMask_en.md).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMaxPool3dWithArgmax](../../pooling/max_pool3d_with_argmax_v2/docs/aclnnMaxPool3dWithArgmax_en.md)|Applies three-dimensional max pooling over the input channels of an input signal, and outputs the value out and indices after pooling.|Defaults to a deterministic implementation.|
| [aclnnMaxPool3dWithArgmaxBackward](../../pooling/max_pool3d_grad_with_argmax/docs/aclnnMaxPool3dWithArgmaxBackward_en.md)|Performs backpropagation of [aclnnMaxPool3dWithArgmax](../../pooling/max_pool3d_with_argmax_v2/docs/aclnnMaxPool3dWithArgmax_en.md).|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMaxUnpool2d](../../index/scatter_elements/docs/aclnnMaxUnpool2d_en.md) | Performs the three-dimensional inverse operation of aclnnMaxPool. outputSize determines the size of out along the H and W axes, fills elements of self in out based on indices, and sets other positions to 0. |Defaults to a deterministic implementation.|
| [aclnnMaxUnpool2dBackward](../../index/gather_elements/docs/aclnnMaxUnpool2dBackward_en.md) | Performs backpropagation of the MaxPool2d inverse operation aclnnMaxUnpool2d, writing element values of gradOutput in out based on indices. |Defaults to a deterministic implementation.|
| [aclnnMaxUnpool3d](../../index/scatter_elements/docs/aclnnMaxUnpool3d_en.md) | Performs the three-dimensional inverse operation of aclnnMaxPool. outputSize determines the size of outRef along the D, H, and W axes, fills elements of self in outRef based on indices, and sets other positions to 0. |Defaults to a deterministic implementation.|
| [aclnnMaxUnpool3dBackward](../../index/gather_elements/docs/aclnnMaxUnpool3dBackward_en.md) | Performs backpropagation of the MaxPool3d inverse operation aclnnMaxUnpool3d, writing element values of gradOutput in out based on indices. |Defaults to a deterministic implementation.|
| [aclnnMedian](../../index/gather_v2/docs/aclnnMedian_en.md) | Returns the median of all elements.|Defaults to a deterministic implementation.|
| [aclnnMedianDim](../../index/gather_v2/docs/aclnnMedianDim_en.md) | Returns the median and position of a specified dimension of a tensor. If the number of elements in the specified dimension is size, the median corresponds to the sorted subscript `(size – 1) // 2`.|Defaults to a deterministic implementation.|
| [aclnnMm](../../matmul/mat_mul_v3/docs/aclnnMm_en.md) |Performs matrix multiplication on the two-dimensional tensors self and mat2.|Defaults to a deterministic implementation.|
| [aclnnMish&aclnnInplaceMish](../../activation/mish/docs/aclnnMish&aclnnInplaceMish_en.md) |Defines a self-regularized non-monotonic neural activation function.|Defaults to a deterministic implementation.|
| [aclnnMishBackward](../../activation/mish_grad/docs/aclnnMishBackward_en.md) |Performs backpropagation of aclnnMish.|Defaults to a deterministic implementation.|
| [aclnnModulate](../../vfusion/modulate/docs/aclnnModulate_en.md) |Implements adaptive scaling and shift of features.|Defaults to a deterministic implementation.|
| [aclnnModulateBackward](../../vfusion/modulate_grad/docs/aclnnModulateBackward_en.md) |Computes the parameters in the backpropagation of ModulateBackward and updates the gradients.|Defaults to a deterministic implementation.|
| [aclnnMseLoss](../../loss/mse_loss/docs/aclnnMseLoss_en.md) | Computes the mean squared error between each element in input x and target y.|Defaults to a deterministic implementation.|
| [aclnnMseLossBackward](../../loss/mse_loss_grad_v2/docs/aclnnMseLossBackward_en.md) | Performs backpropagation of mean squared error function aclnnMseLoss.|Defaults to a deterministic implementation.|
| [aclnnMseLossOut](../../loss/mse_loss/docs/aclnnMseLossOut_en.md)|Computes the mean squared error between each element in input x and target y. reduction specifies the reduction to be applied to the output. The value can be none, mean, or sum. mean indicates that the output will be averaged by reducing axis 0, and sum indicates that the output will be summed by reducing axis 0.|Defaults to a deterministic implementation.|
| [aclnnMultilabelMarginLoss](../../loss/multilabel_margin_loss/docs/aclnnMultilabelMarginLoss_en.md) | Computes the negative log-likelihood loss.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMultiScaleDeformableAttnFunction](../../vfusion/multi_scale_deformable_attn_function/docs/aclnnMultiScaleDeformableAttnFunction_en.md)|Traverses different sampling points of feature maps with different sizes by specifying parameters.|Defaults to a deterministic implementation.|
| [aclnnMultiScaleDeformableAttentionGrad](../../vfusion/multi_scale_deformable_attention_grad/docs/aclnnMultiScaleDeformableAttentionGrad_en.md)|The forward operator traverses different sampling points of feature maps with different sizes by specifying parameters. The reverse operator calculates the gradient corresponding to the input based on the contribution of the forward input to the output and an initial gradient.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMv](../../matmul/mv/docs/aclnnMv_en.md) |Computes the product of matrix input and vector vec.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnMedian](../../index/gather_v2/docs/aclnnNanMedian_en.md) | Returns the median of all elements after NAN is ignored.|Defaults to a deterministic implementation.|
| [aclnnNanMedianDim](../../index/gather_v2/docs/aclnnNanMedianDim_en.md) | Returns the median and position of values in the specified dimension of a tensor after NAN is ignored.|Defaults to a deterministic implementation.|
| [aclnnNorm](../../norm/lp_norm_v2/docs/aclnnNorm_en.md)|Returns the matrix norm or vector norm of a given tensor.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnNLLLoss](../../loss/nll_loss/docs/aclnnNLLLoss_en.md) | Computes the negative log-likelihood loss.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnNLLLoss2d](../../loss/nll_loss/docs/aclnnNLLLoss2d_en.md) | Computes the negative log-likelihood loss.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnNLLLoss2dBackward](../../loss/nll_loss_grad/docs/aclnnNLLLoss2dBackward_en.md) | Performs backpropagation of the negative log-likelihood loss.|Defaults to a deterministic implementation.|
| [aclnnNLLLossBackward](../../loss/nll_loss_grad/docs/aclnnNLLLossBackward_en.md) | Performs backpropagation of the negative log-likelihood loss function.|Defaults to a deterministic implementation.|
| [aclnnNonzero](../../index/non_zero/docs/aclnnNonzero_en.md) | Obtains the inverse matrix of an array.|Defaults to a deterministic implementation.|
| [aclnnNonzeroV2](../../index/non_zero/docs/aclnnNonzeroV2_en.md) | Finds the positions of non-zero elements in self. Assume that the dimension of self is D and the number of non-zero elements in self is N. The shape of out is D × N. Each column indicates the position coordinates of a non-zero element.|Defaults to a deterministic implementation.|
| aclnnObfuscationCalculate | Sends tensor x and configuration parameters (such as param and cmd) to the PMCC obfuscation engine. The CA module of the engine calls the TA module to perform tensor obfuscation and returns the obfuscated tensor y with the same shape as x.|Defaults to a deterministic implementation.|
| aclnnObfuscationCalculateV2 | Sends tensor x and configuration parameters (such as param and cmd) to the PMCC obfuscation engine. The CA module of the engine calls the TA module to perform tensor obfuscation and returns the obfuscated tensor y with the same shape as x.|Defaults to a deterministic implementation.|
| aclnnObfuscationSetup | Initializes and releases resources of the PMCC model obfuscation engine.|Defaults to a deterministic implementation.|
| aclnnObfuscationSetupV2 | Initializes and releases resources of the PMCC model obfuscation engine.|Defaults to a deterministic implementation.|
| [aclnnPrelu](../../activation/prelu/docs/aclnnPrelu_en.md) |Performs an activation function. If a value in a tensor is greater than 0, the value is used. Otherwise, the product of the weight and value is used.|Defaults to a deterministic implementation.|
| [aclnnPreluBackward](../../activation/prelu_grad_update/docs/aclnnPreluBackward_en.md) |Performs backpropagation of aclnnPreluBackward.|Defaults to a deterministic implementation.|
| [aclnnQuantConvolution](../../conv/convolution_forward/docs/aclnnQuantConvolution_en.md) |Performs two-dimensional and three-dimensional convolution for per-channel quantization.|Defaults to a deterministic implementation.|
| [aclnnQuantize](../../quant/quantize/docs/aclnnQuantize_en.md)|Quantizes the input tensor.|Defaults to a deterministic implementation.|
| [aclnnQuantizeBatchNorm](../../norm/quantized_batch_norm/docs/aclnnQuantizedBatchNorm_en.md)|Dequantizes the input Tensor, normalizes the Tensor based on the input weight, bias, and epsilon, and then quantizes the Tensor based on the outputScale and outputZeroPoint.|Defaults to a deterministic implementation.|
| [aclnnQuantMatmul](../../matmul/quant_matmul/docs/aclnnQuantMatmul_en.md)|Performs matrix multiplication on tensors self and mat2 (matrix multiplication from one to six dimensions is supported).|Defaults to a deterministic implementation.|
| [aclnnQuantMatmulDequant](../../matmul/quant_matmul/docs/aclnnQuantMatmul_en.md)|Quantizes the input x, performs matrix multiplication, and dequantizes the result.|Defaults to a deterministic implementation.|
| [aclnnQuantMatmulV2](../../matmul/quant_matmul/docs/aclnnQuantMatmulV2_en.md)|Performs quantized matrix multiplication, supporting at most three-dimensional input. Similar APIs include aclnnMm (only two-dimensional tensors can be used as the input of matrix multiplication) and aclnnBatchMatMul (only three-dimensional matrix multiplication is supported, whose first dimension is the batch dimension).|Defaults to a deterministic implementation.|
| [aclnnQuantMatmulV3](../../matmul/quant_batch_matmul_v3/docs/aclnnQuantMatmulV3_en.md) |Performs quantized matrix multiplication, supporting at least two-dimensional input and at most six-dimensional-dimensional input.|Defaults to a deterministic implementation.|
| [aclnnQuantMatmulV4](../../matmul/quant_batch_matmul_v3/docs/aclnnQuantMatmulV4_en.md) |Performs quantized matrix multiplication, supporting at least two-dimensional input and at most six-dimensional-dimensional input.|Defaults to a deterministic implementation.|
| [aclnnQuantMatmulV5](../../matmul/quant_batch_matmul_v4/docs/aclnnQuantMatmulV5_en.md) |Performs matrix multiplication for quantization.|Defaults to a deterministic implementation.|
| [aclnnQuantMatmulReduceSumWeightNz](../../matmul/quant_matmul_reduce_sum/docs/aclnnQuantMatmulReduceSumWeightNz_en.md) |Performs quantized group matrix computation, adds the matrix computation results of all groups, and outputs the result.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnQuantMatmulWeightNz](../../matmul/quant_batch_matmul_v3/docs/aclnnQuantMatmulWeightNz_en.md) |Performs matrix multiplication for quantization.|Defaults to a deterministic implementation.|
| [aclnnRelu&aclnnInplaceRelu](../../activation/relu/docs/aclnnRelu&aclnnInplaceRelu_en.md) |Performs an activation function, which returns a tensor with the same shape as the input tensor. If value of the tensor is greater than or equal to 0, the value is used. If the value is less than 0, 0 is used.|Defaults to a deterministic implementation.|
| [aclnnRenorm&aclnnInplaceRenorm](../../norm/renorm/docs/aclnnRenorm&aclnnInplaceRenorm_en.md)|Returns a tensor where each sub-tensor of the input tensor self along the dimension dim is normalized so that the p-norm of the sub-tensor is less than maxNorm.|Defaults to a deterministic implementation.|
| [aclnnRepeatInterleave](../../index/repeat_interleave/docs/aclnnRepeatInterleave_en.md) | Description: Repeats the tensor for the number of times specified by repeats after the tensor self is flattened.|Defaults to a deterministic implementation.|
| aclnnRepeatInterleaveInt | Repeats each element for the number of times specified by repeats after the tensor self is flattened.|Defaults to a deterministic implementation.|
| aclnnRepeatInterleaveIntWithDim | Repeats each element in the tensor based on the dim dimension for the corresponding number of times specified by repeats.|Defaults to a deterministic implementation.|
| aclnnRepeatInterleaveTensor | For the i-th element of tensor repeats, repeats i for repeats[i] times.|Defaults to a deterministic implementation.|
| aclnnRepeatInterleaveWithDim | Repeats each element in the tensor based on the dim dimension for the corresponding number of times specified by the tensor repeats.|Defaults to a deterministic implementation.|
| aclnnRepeatInterleaveGrad | Performs backpropagation for the repeatInterleave operator, and performs ReduceSum on the axis dimension of the yGrad tensor based on repeats.|Defaults to a deterministic implementation.|
| [aclnnRmsNorm](../../norm/rms_norm/docs/aclnnRmsNorm_en.md)|The RmsNorm operator is a normalization operation commonly used in foundation models. Compared with the LayerNorm operator, the RmsNorm operator does not subtract the average value.|Defaults to a deterministic implementation.|
| [aclnnRmsNormGrad](../../norm/rms_norm_grad/docs/aclnnRmsNormGrad_en.md)|Performs backpropagation of [aclnnRmsNorm](../../norm/rms_norm/docs/aclnnRmsNorm_en.md). It is used to compute the gradient of RMSNorm, that is, compute the gradient of the input tensor during backpropagation.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnRmsNormQuant]()|Fuses the Add operator before aclnnRmsNorm and the Quantize operator after aclnnRmsNorm to reduce move-in and move-out operations.|Defaults to a deterministic implementation.|
| [aclnnRReluWithNoise&aclnnInplaceRReluWithNoise](../../activation/leaky_relu/docs/aclnnRReluWithNoise&aclnnInplaceRReluWithNoise_en.md) |Performs the activation function of the random rectified linear unit with noise. When the input is less than or equal to 0, the slope is a. When the input is greater than 0, the slope is 1.|Defaults to a deterministic implementation.|
| [aclnnScatter&aclnnInplaceScatter](../../index/scatter_elements_v2/docs/aclnnScatter&aclnnInplaceScatter_en.md) |Replaces, accumulates, or multiplies the values in the tensor src to the tensor self based on the given axis and direction and the corresponding position relationship.|Defaults to a deterministic implementation.|
| [aclnnScatterAdd](../../index/scatter_add/docs/aclnnScatterAdd_en.md) |Adds values from the src tensor into the self tensor in sequence according to the specified axis direction and the corresponding position specified by the index tensor. If more than one src value is written at the same position in self, these values are accumulated at this position.|Defaults to a deterministic implementation.|
| [aclnnScatterNd](../../index/scatter_nd_update/docs/aclnnScatterNd_en.md) | Copies the data from data to out, and updates the data in out based on updates at the specified indices.|Defaults to a deterministic implementation.|
| [aclnnScatterNdUpdate](../../index/scatter_nd_update/docs/aclnnScatterNdUpdate_en.md) | Updates the values in varRef in sequence by referring to the values in the updates tensor based on the specified indices.|Defaults to a deterministic implementation.|
| aclnnScatterValue&aclnnInplaceScatterValue | Writes values from scalar value into tensor self one by one according to the specified axis, direction, and corresponding position relationship. value will be broadcast into tensor src whose shape is the same as that of tensor index for Scatter computation.|Defaults to a deterministic implementation.|
| [aclnnScaledMaskedSoftmax](../../vfusion/scaled_masked_softmax_v2/docs/aclnnScaledMaskedSoftmax_en.md)|Scales and masks the input x, and then performs the softmax operation to obtain the output.|Defaults to a deterministic implementation.|
| [aclnnScaledMaskedSoftmaxBackward](../../vfusion/scaled_masked_softmax_grad_v2/docs/aclnnScaledMaskedSoftmaxBackward_en.md)|Performs backpropagation of softmax, and scales and masks the result.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnSelu&aclnnInplaceSelu](../../activation/selu/docs/aclnnSelu&aclnnInplaceSelu_en.md) |Computes the Selu sign function element-wise on the input tensor and outputs the result tensor.|Defaults to a deterministic implementation.|
| [aclnnSeluBackward](../../activation/selu_grad/docs/aclnnSeluBackward_en.md) |Performs backpropagation of aclnnSelu.|Defaults to a deterministic implementation.|
| [aclnnShrink](../../activation/shrink/docs/aclnnShrink_en.md) |Performs non-linear transformation on the input tensor, and applies scaling and bias processing to the input using bias based on the relationship between input self and threshold lambd.|Defaults to a deterministic implementation.|
| [aclnnSigmoid&aclnnInplaceSigmoid](../../activation/sigmoid/docs/aclnnSigmoid&aclnnInplaceSigmoid_en.md) |Performs the sigmoid operation on the input tensor.|Defaults to a deterministic implementation.|
| [aclnnSigmoidBackward](../../activation/sigmoid_grad/docs/aclnnSigmoidBackward_en.md) |Performs backpropagation of sigmoid. The sigmoid gradient input is computed based on the sigmoid backpropagation gradient and forward output.|Defaults to a deterministic implementation.|
| [aclnnSilu](../../activation/swish/docs/aclnnSilu_en.md) |This operator is also called the Swish function.|Defaults to a deterministic implementation.|
| [aclnnSiluBackward](../../activation/silu_grad/docs/aclnnSiluBackward_en.md) |Performs backpropagation of aclnnSilu, and computes the silu gradient input based on the silu backpropagation gradient and forward output.|Defaults to a deterministic implementation.|
| aclnnSilentCheck |Compares the input feature value (val) with the absolute threshold and relative threshold to determine whether to trigger a silence check fault.|Defaults to a deterministic implementation.|
| aclnnSilentCheckV2 |Compare val with the new Markov inequality threshold for verification based on the stepRef parameter.|Defaults to a deterministic implementation.|
| [aclnnSmoothL1Loss](../../loss/smooth_l1_loss_v2/docs/aclnnSmoothL1Loss_en.md) | Computes the SmoothL1 loss function.|Defaults to a deterministic implementation.|
| [aclnnSmoothL1LossBackward](../../loss/smooth_l1_loss_grad_v2/docs/aclnnSmoothL1LossBackward_en.md) | Performs backpropagation of aclnnSmoothL1Loss API.|Defaults to a deterministic implementation.|
| [aclnnSoftMarginLoss](../../loss/soft_margin_loss/docs/aclnnSoftMarginLoss_en.md) | Computes the binary logic loss function of inputs self and target.|Defaults to a deterministic implementation.|
| [aclnnSoftMarginLossBackward](../../loss/soft_margin_loss_grad/docs/aclnnSoftMarginLossBackward_en.md) | Performs backpropagation of the aclnnSoftMarginLoss binary logic loss function.|Defaults to a deterministic implementation.|
| [aclnnSoftmax](../../activation/softmax_v2/docs/aclnnSoftmax_en.md) |Computes the softmax value of the input tensor.|Defaults to a deterministic implementation.|
| [aclnnSoftmaxBackward](../../activation/softmax_grad/docs/aclnnSoftmaxBackward_en.md) |Performs backpropagation of softmax.|Defaults to a deterministic implementation.|
| [aclnnSoftmaxCrossEntropyWithLogits](../../activation/softmax_cross_entropy_with_logits/docs/aclnnSoftmaxCrossEntropyWithLogits_en.md) |Computes the softmax and cross-entropy loss and provides the backward gradient for the input logits.|Defaults to a deterministic implementation.|
| [aclnnSoftplus](../../activation/softplus_v2/docs/aclnnSoftplus_en.md) |Performs activation function softplus.|Defaults to a deterministic implementation.|
| [aclnnSoftplusBackward](../../activation/softplus_v2_grad/docs/aclnnSoftplusBackward_en.md) |Performs backpropagation of aclnnSoftplus.|Defaults to a deterministic implementation.|
| [aclnnSoftshrink](../../activation/softshrink/docs/aclnnSoftshrink_en.md) |Forcibly shrinks elements in the range of λ by element.|Defaults to a deterministic implementation.|
| [aclnnSoftshrinkBackward](../../activation/softshrink_grad/docs/aclnnSoftshrinkBackward_en.md) |Performs backpropagation of the Softshrink function.|Defaults to a deterministic implementation.|
| [aclnnSquaredRelu](../../activation/squared_relu/docs/aclnnSquaredRelu_en.md) |The SquaredReLU function is a variant of the standard ReLU function. It is mainly used to square the output of the ReLU function and is often used as the activation function of a model.|Defaults to a deterministic implementation.|
| [aclnnSwiGlu](../../activation/swi_glu/docs/aclnnSwiGlu_en.md) |Performs a Swish Gated Linear Unit (SwiGlu) activation function, and implements the SwiGlu computation of x.|Defaults to a deterministic implementation.|
| [aclnnSwiGluGrad](../../activation/swi_glu_grad/docs/aclnnSwiGluGrad_en.md) |Performs backpropagation of aclnnSwiGlu and computes the SwiGlu reverse gradient of x.|Defaults to a deterministic implementation.|
| [aclnnSwiGluQuant](../../quant/swi_glu_quant/docs/aclnnSwiGluQuant_en.md)|Adds quantization after the SwiGlu activation function to implement SwiGluQuant computation of the input x.|Defaults to a deterministic implementation.|
| [aclnnSwiGluQuantV2](../../quant/swi_glu_quant/docs/aclnnSwiGluQuantV2_en.md)|Adds quantization after the SwiGlu activation function to implement SwiGluQuant computation of the input x. The int8 or int4 quantization output is supported.|Defaults to a deterministic implementation.|
| [aclnnSwish](../../activation/swish/docs/aclnnSwish_en.md) |Performs the Swish activation function to the input tensor element-wise and outputs the result tensor.|Defaults to a deterministic implementation.|
| [aclnnSwishBackward](../../activation/swish_grad/docs/aclnnSwishBackward_en.md) |Performs backpropagation of aclnnSwish. It is used to compute the gradient of the Swish activation function.|Defaults to a deterministic implementation.|
| [aclnnSyncBatchNormGatherStats](../../norm/sync_batch_norm_gather_stats/docs/aclnnSyncBatchNormGatherStats_en.md) |Collects the mean value and variance of data on all devices and updates the global mean value and variance.|Defaults to a deterministic implementation.|
| [aclnnTake](../../index/gather_v2/docs/aclnnTake_en.md) | Takes the input self tensor as a one-dimensional array, uses index to obtain a value from self, and outputs a tensor whose shape is the same as index.|Defaults to a deterministic implementation.|
| aclnnTfScatterAdd |Implements the functions compatible with tf.compat.v1.scatter_add and tf.compat.v1.scatter_nd_add. Adds the values in tensor updates to the slices of tensor varRef based on the specified index tensor indices. If more than one updates value is written to the same slice of varRef, these values are accumulated at this slice.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnThreshold&aclnnInplaceThreshold](../../activation/threshold/docs/aclnnThreshold&aclnnInplaceThreshold_en.md) |Sets the threshold for input x. If elements in x are greater than threshold, the elements are returned. Otherwise, value is returned.|Defaults to a deterministic implementation.|
| [aclnnThresholdBackward](../../activation/threshold_grad_v2_d/docs/aclnnThresholdBackward_en.md) |Performs backpropagation of aclnnThreshold.|Defaults to a deterministic implementation.|
| [aclnnTransposeBatchMatMul](../../matmul/transpose_batch_mat_mul/docs/aclnnTransposeBatchMatMul_en.md) |Performs matrix multiplication on tensors x1 and x2.|Defaults to a deterministic implementation.|
| [aclnnTransQuantParam](../../quant/trans_quant_param/docs/aclnnTransQuantParam_en.md)|Converts the input scale data from FLOAT32 type to UINT64 type required by the hardware, and stores the data in quantParam.|Defaults to a deterministic implementation.|
| [aclnnTransQuantParamV2](../../quant/trans_quant_param_v2/docs/aclnnTransQuantParamV2_en.md)|Converts the data type of the quantization parameter scale from FLOAT32 to UINT64 or INT64 required by the hardware.|Defaults to a deterministic implementation.|
| [aclnnTransQuantParamV3](../../quant/trans_quant_param_v2/docs/aclnnTransQuantParamV3_en.md)|Converts the data type of the quantization parameter scale from Float32 to UINT64 or INT64 required by the hardware.|Defaults to a deterministic implementation.|
| aclnnTopKTopPSample |Performs topK-topP sampling computation based on the input logits, topK/topP sampling parameters, and random sampling weight distribution q, and outputs the maximum word frequency logitsSelectIdx of each batch and the word frequency distribution logitsTopKPSelect after topK-topP sampling.|Defaults to a deterministic implementation.|
| [aclnnUnique](../../index/scatter_elements/docs/aclnnUnique_en.md) |Returns the unique elements of the input tensor.|Defaults to a deterministic implementation.|
| [aclnnUnique2](../../index/scatter_elements/docs/aclnnUnique2_en.md) |Removes duplicate elements from the input tensor self and returns the unique elements in self. This is an enhanced unique function, with a new return value countsOut indicating the number of occurrences for where elements in the input self map to in valueOut, controlled by the returnCounts parameter.|Defaults to a deterministic implementation.|
| [aclnnUniqueConsecutive](../../index/unique_consecutive/docs/aclnnUniqueConsecutive_en.md) |Eliminates all but the first element from every consecutive group of equivalent elements. When dim is not empty, tensors except the first tensor from every consecutive group of equivalent elements are eliminated along the corresponding dimension.|Defaults to a deterministic implementation.|
| [aclnnUniqueDim](../../index/unique_with_counts_ext2/docs/aclnnUniqueDim_en.md) |Removes duplicates from the input tensor self along a given dim.|Defaults to a deterministic implementation.|
| [aclnnWeightQuantBatchMatmul](../../matmul/weight_quant_batch_matmul/docs/aclnnWeightQuantBatchMatmul_en.md)|Quantizes mat2 in self * mat2 (matmul/batchmatmul), which is a process of fake-quantization.|Defaults to a deterministic implementation.|
| [aclnnWeightQuantBatchMatmulV2](../../matmul/weight_quant_batch_matmul_v2/docs/aclnnWeightQuantBatchMatmulV2_en.md) |Performs input matrix multiplication in a fake-quantization scenario and implements output quantization.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
| [aclnnWeightQuantBatchMatmulV3](../../matmul/weight_quant_batch_matmul_v2/docs/aclnnWeightQuantBatchMatmulV3_en.md) |Performs input matrix multiplication in a fake-quantization scenario and implements output quantization.|Defaults to a non-deterministic implementation. Enabling deterministic computing is supported.|
