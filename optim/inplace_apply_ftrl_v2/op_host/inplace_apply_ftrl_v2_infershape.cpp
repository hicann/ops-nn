/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 * Generated By CANNBot
 */

#include "register/op_impl_registry.h"
#include "exe_graph/runtime/infer_shape_context.h"
#include "op_common/log/log.h"

using namespace ge;

namespace ops {

static constexpr size_t MAX_DIM_NUM = 8;
static constexpr size_t NUM_TENSOR_INPUTS = 4;

static ge::graphStatus InferShape4InplaceApplyFtrlV2(gert::InferShapeContext* context)
{
    const gert::Shape* varShape = context->GetInputShape(0);
    OP_CHECK_NULL_WITH_CONTEXT(context, varShape);
    const gert::Shape* accumShape = context->GetInputShape(1);
    OP_CHECK_NULL_WITH_CONTEXT(context, accumShape);
    const gert::Shape* linearShape = context->GetInputShape(2);
    OP_CHECK_NULL_WITH_CONTEXT(context, linearShape);
    const gert::Shape* gradShape = context->GetInputShape(3);
    OP_CHECK_NULL_WITH_CONTEXT(context, gradShape);

    if (*varShape != *accumShape || *varShape != *linearShape || *varShape != *gradShape) {
        OP_LOGE(context, "var, accum, linear, grad shapes must be identical");
        return ge::GRAPH_FAILED;
    }
    for (size_t i = 0; i < NUM_TENSOR_INPUTS; i++) {
        const gert::Shape* shape = context->GetInputShape(i);
        OP_CHECK_NULL_WITH_CONTEXT(context, shape);
        if (shape->GetDimNum() > MAX_DIM_NUM) {
            OP_LOGE(context, "input %zu dim num %zu exceeds max %zu", i, shape->GetDimNum(), MAX_DIM_NUM);
            return ge::GRAPH_FAILED;
        }
    }

    gert::Shape* outVarShape = context->GetOutputShape(0);
    OP_CHECK_NULL_WITH_CONTEXT(context, outVarShape);
    gert::Shape* outAccumShape = context->GetOutputShape(1);
    OP_CHECK_NULL_WITH_CONTEXT(context, outAccumShape);
    gert::Shape* outLinearShape = context->GetOutputShape(2);
    OP_CHECK_NULL_WITH_CONTEXT(context, outLinearShape);

    *outVarShape = *varShape;
    *outAccumShape = *accumShape;
    *outLinearShape = *linearShape;

    return ge::GRAPH_SUCCESS;
}

IMPL_OP_INFERSHAPE(InplaceApplyFtrlV2).InferShape(InferShape4InplaceApplyFtrlV2);

} // namespace ops
