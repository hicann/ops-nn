/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 * Generated By CANNBot
 */

/*!
 * \file inplace_apply_ftrl_v2_proto.h
 * \brief InplaceApplyFtrlV2 GE IR 图模式注册
 *
 * 9 输入（4 tensor + 5 scalar-as-tensor）+ 3 输出（inplace）
 * 兼容 TensorFlow raw_ops.ApplyFtrlV2，原地更新版本
 */

#ifndef OPS_OP_PROTO_INC_INPLACE_APPLY_FTRL_V2_H_
#define OPS_OP_PROTO_INC_INPLACE_APPLY_FTRL_V2_H_

#include "graph/operator_reg.h"

namespace ge {
/**
 *@brief InplaceApplyFtrlV2 optimizer update: FTRL-Proximal 在线学习单步参数更新（原地版本）。
 *@par Inputs:
 * @li var: A mutable Tensor. Must be one of DT_BF16, DT_FLOAT16, DT_FLOAT.
 *     Should be a Variable Tensor.
 * @li accum: A mutable Tensor of the same type as "var".
 *     Should be a Variable Tensor.
 * @li linear: A mutable Tensor of the same type as "var".
 *     Should be a Variable Tensor.
 * @li grad: A Tensor of the same type as "var", for the gradient.
 * @li lr: A Tensor of the same type as "var", for the scaling factor. Must be a scalar.
 * @li l1: A Tensor of the same type as "var", for L1 regularization. Must be a scalar.
 * @li l2: A Tensor of the same type as "var", for L2 regularization. Must be a scalar.
 * @li l2_shrinkage: A Tensor of the same type as "var".
 * @li lr_power: A Tensor of the same type as "var", for the scaling factor. Must be a scalar.
 *
 *@par Attributes:
 * @li use_locking: An optional bool. Defaults to "False".
 *     If "True", updating of the "var" and "accum" tensors will be
 *     protected by a lock; otherwise the behavior is undefined,
 *     but may exhibit less contention.
 *
 *@par Outputs:
 * @li var: A mutable Tensor. Has the same type as "var".
 * @li accum: A mutable Tensor. Has the same type as "var".
 * @li linear: A mutable Tensor. Has the same type as "var".
 *
 *@par Third-party framework compatibility
 * Compatible with TensorFlow raw_ops.ApplyFtrlV2.
 */

REG_OP(InplaceApplyFtrlV2)
    .INPUT(var, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(accum, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(linear, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(grad, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(lr, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(l1, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(l2, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(l2_shrinkage, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(lr_power, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(var, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(accum, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(linear, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyFtrlV2)

} // namespace ge

#endif // OPS_OP_PROTO_INC_INPLACE_APPLY_FTRL_V2_H_
