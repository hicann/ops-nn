/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <cstdint>
#include <cstring>
#include "gtest/gtest.h"

#ifdef __CCE_KT_TEST__
#include "tikicpulib.h"
#endif

#include "../../../op_kernel/arch35/inplace_apply_ftrl_v2_tiling_data.h"

using namespace std;

extern "C" __global__ __aicore__ void inplace_apply_ftrl_v2(GM_ADDR var, GM_ADDR accum, GM_ADDR linear, GM_ADDR grad,
                                                            GM_ADDR lr, GM_ADDR l1, GM_ADDR l2, GM_ADDR l2_shrinkage,
                                                            GM_ADDR lr_power, GM_ADDR varOut, GM_ADDR accumOut,
                                                            GM_ADDR linearOut, GM_ADDR workspace, GM_ADDR tiling);

class InplaceApplyFtrlV2KernelTest : public testing::Test {
protected:
    static void SetUpTestCase() { cout << "InplaceApplyFtrlV2KernelTest SetUp" << endl; }
    static void TearDownTestCase() { cout << "InplaceApplyFtrlV2KernelTest TearDown" << endl; }
};

TEST_F(InplaceApplyFtrlV2KernelTest, test_fp32_basic)
{
    constexpr size_t numElements = 256;
    constexpr uint32_t blockDim = 1;
    const size_t dataSize = numElements * sizeof(float);
    const size_t scalarSize = sizeof(float);

    auto* var = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* accum = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* linear = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* grad = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* lr = static_cast<uint8_t*>(AscendC::GmAlloc(scalarSize));
    auto* l1 = static_cast<uint8_t*>(AscendC::GmAlloc(scalarSize));
    auto* l2 = static_cast<uint8_t*>(AscendC::GmAlloc(scalarSize));
    auto* l2Shrinkage = static_cast<uint8_t*>(AscendC::GmAlloc(scalarSize));
    auto* lrPower = static_cast<uint8_t*>(AscendC::GmAlloc(scalarSize));
    auto* varOut = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* accumOut = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* linearOut = static_cast<uint8_t*>(AscendC::GmAlloc(dataSize));
    auto* workspace = static_cast<uint8_t*>(AscendC::GmAlloc(1024));
    auto* tiling = static_cast<uint8_t*>(AscendC::GmAlloc(sizeof(InplaceApplyFtrlV2TilingData)));

    auto* varData = reinterpret_cast<float*>(var);
    auto* accumData = reinterpret_cast<float*>(accum);
    auto* linearData = reinterpret_cast<float*>(linear);
    auto* gradData = reinterpret_cast<float*>(grad);
    for (size_t i = 0; i < numElements; ++i) {
        varData[i] = 0.5f;
        accumData[i] = 0.3f;
        linearData[i] = 0.1f;
        gradData[i] = 0.01f;
    }
    *reinterpret_cast<float*>(lr) = 0.1f;
    *reinterpret_cast<float*>(l1) = 0.01f;
    *reinterpret_cast<float*>(l2) = 0.001f;
    *reinterpret_cast<float*>(l2Shrinkage) = 0.0f;
    *reinterpret_cast<float*>(lrPower) = -0.5f;

    auto* tilingData = reinterpret_cast<InplaceApplyFtrlV2TilingData*>(tiling);
    tilingData->totalElements = numElements;
    tilingData->blockFactor = numElements;
    tilingData->ubFactor = numElements;

    AscendC::SetKernelMode(KernelMode::AIV_MODE);
    ICPU_RUN_KF(inplace_apply_ftrl_v2, blockDim, var, accum, linear, grad, lr, l1, l2, l2Shrinkage, lrPower, varOut,
                accumOut, linearOut, workspace, tiling);

    AscendC::GmFree(var);
    AscendC::GmFree(accum);
    AscendC::GmFree(linear);
    AscendC::GmFree(grad);
    AscendC::GmFree(lr);
    AscendC::GmFree(l1);
    AscendC::GmFree(l2);
    AscendC::GmFree(l2Shrinkage);
    AscendC::GmFree(lrPower);
    AscendC::GmFree(varOut);
    AscendC::GmFree(accumOut);
    AscendC::GmFree(linearOut);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);
}
