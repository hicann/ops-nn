/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 */
/*!
 * \file inplace_apply_ada_max_vf.h
 * \brief InplaceApplyAdaMax Vector Function（计算链）
 *
 * 实现 AdaMax 更新链的 fused 向量计算：
 *   m_out = β1·m + (1-β1)·grad
 *   v_out = max(β2·v, |grad|)
 *   var_out = var - lr_adj · m_out / (v_out + ε)
 *
 * 使用 PRECISION_0ULP_FTZ_FALSE Div 确保 IEEE 754 0-ULP 精度，匹配 numpy golden。
 */

#ifndef INPLACE_APPLY_ADA_MAX_VF_H
#define INPLACE_APPLY_ADA_MAX_VF_H

#include "kernel_operator.h"

static constexpr AscendC::Reg::DivSpecificMode kHighPrecDiv = {AscendC::Reg::MaskMergeMode::ZEROING, true,
                                                               AscendC::DivAlgo::PRECISION_0ULP_FTZ_FALSE};

template <typename T>
__simd_vf__ inline void AdaMaxVF(__ubuf__ T* varAddr, __ubuf__ T* mAddr, __ubuf__ T* vAddr, __ubuf__ T* gradAddr,
                                 T beta1, T oneMinusBeta1, T beta2, T epsilon, T lrAdj, uint32_t count,
                                 uint32_t oneRepeatSize, uint16_t repeatTimes)
{
    AscendC::Reg::RegTensor<T> regVar, regM, regV, regGrad, regTmp;
    AscendC::Reg::MaskReg mask;

    for (uint16_t i = 0; i < repeatTimes; ++i) {
        uint32_t off = i * oneRepeatSize;
        uint32_t rem = count - off;
        mask = AscendC::Reg::UpdateMask<T>(rem);

        AscendC::Reg::LoadAlign(regVar, varAddr + off);
        AscendC::Reg::LoadAlign(regM, mAddr + off);
        AscendC::Reg::LoadAlign(regV, vAddr + off);
        AscendC::Reg::LoadAlign(regGrad, gradAddr + off);

        AscendC::Reg::Abs(regTmp, regGrad, mask);

        AscendC::Reg::Muls(regGrad, regGrad, oneMinusBeta1, mask);
        AscendC::Reg::Muls(regM, regM, beta1, mask);
        AscendC::Reg::Add(regM, regM, regGrad, mask);

        AscendC::Reg::Muls(regV, regV, beta2, mask);
        AscendC::Reg::Max(regV, regV, regTmp, mask);

        AscendC::Reg::Adds(regTmp, regV, epsilon, mask);
        AscendC::Reg::Div<T, &kHighPrecDiv>(regTmp, regM, regTmp, mask);

        AscendC::Reg::Muls(regTmp, regTmp, lrAdj, mask);
        AscendC::Reg::Sub(regVar, regVar, regTmp, mask);

        AscendC::Reg::StoreAlign(varAddr + off, regVar, mask);
        AscendC::Reg::StoreAlign(mAddr + off, regM, mask);
        AscendC::Reg::StoreAlign(vAddr + off, regV, mask);
    }
}

template <typename T>
__aicore__ inline void CallAdaMaxVF(__ubuf__ T* varAddr, __ubuf__ T* mAddr, __ubuf__ T* vAddr, __ubuf__ T* gradAddr,
                                    T beta1, T oneMinusBeta1, T beta2, T epsilon, T lrAdj, int64_t count)
{
    constexpr uint32_t ONE_REPEAT_SIZE = AscendC::GetVecLen() / sizeof(float);
    uint16_t repeatTimes = AscendC::CeilDivision(static_cast<uint64_t>(count), ONE_REPEAT_SIZE);
    asc_vf_call<AdaMaxVF<T>>(varAddr, mAddr, vAddr, gradAddr, beta1, oneMinusBeta1, beta2, epsilon, lrAdj,
                             static_cast<uint32_t>(count), ONE_REPEAT_SIZE, repeatTimes);
}

#endif // INPLACE_APPLY_ADA_MAX_VF_H
