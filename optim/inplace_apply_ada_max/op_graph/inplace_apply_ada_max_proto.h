/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/*!
 * \file inplace_apply_ada_max_proto.h
 * \brief InplaceApplyAdaMax 图模式算子原型定义
 *
 * 9 输入 + 3 输出 (var/m/v，输出名与输入同名 = GE inplace 别名) + 1 属性 (use_locking)。
 * 与 op_host/inplace_apply_ada_max_def.cpp 保持同步。
 */
#ifndef INPLACE_APPLY_ADA_MAX_PROTO_H
#define INPLACE_APPLY_ADA_MAX_PROTO_H

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
 *@brief Updates "var" according to the AdaMax algorithm.
 *  t-1 mean previous period.
 *  m_t <- beta1 * m{t-1} + (1 - beta1) * grad
 *  v_t <- max(beta2 * v{t-1}, abs(grad))
 *  var <- var - lr / (1 - beta1^t) * m_t / (v_t + epsilon)
 *
 *@attention Constraints:
 *  The input tensors must have the same shape.
 *  The output "var", "m", and "v" are in-place outputs that alias the
 *  corresponding inputs, i.e., the input buffers are updated directly.
 *
 *@par Inputs:
 *@li var: A mutable tensor. Must be one of the following types: float16, float32.
 *     Should be from a Variable().
 *@li m: A mutable tensor. Has the same type as "var".
 *     Should be from a Variable().
 *@li v: A mutable tensor. Has the same type as "var".
 *     Should be from a Variable().
 *@li beta1_power: A scalar. Has the same type as "var".
 *@li lr: learning_rate. A scalar. Has the same type as "var".
 *@li beta1: A scalar. Has the same type as "var".
 *@li beta2: A scalar. Has the same type as "var".
 *@li epsilon: A scalar. Has the same type as "var".
 *@li grad: A tensor for the gradient. Has the same type as "var".
 *
 *@par Attributes:
 * use_locking: An optional bool. Defaults to "False".
 *     If "True", updating of the "var", "m", and "v" tensors is protected
 *     by a lock; otherwise the behavior is undefined, but may exhibit less
 *     contention.
 *
 *@par Outputs:
 *@li var: A mutable tensor. Has the same type as input "var".
 *     In-place output aliasing input "var".
 *@li m: A mutable tensor. Has the same type as input "m".
 *     In-place output aliasing input "m".
 *@li v: A mutable tensor. Has the same type as input "v".
 *     In-place output aliasing input "v".
 *
 *@par Third-party framework compatibility
 * Compatible with the TensorFlow operator ApplyAdaMax.
 */
REG_OP(InplaceApplyAdaMax)
    .INPUT(var, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(m, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(v, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(beta1_power, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(lr, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(beta1, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(beta2, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(epsilon, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(grad, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(var, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(m, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(v, TensorType({DT_FLOAT16, DT_FLOAT}))
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyAdaMax)
} // namespace ge

#endif // INPLACE_APPLY_ADA_MAX_PROTO_H
