/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file apply_came_part2_infershape.cpp
 * \brief
 */
#include "log/log.h"
#include "register/op_impl_registry.h"
#include <sstream>

using namespace ge;
using namespace gert;

namespace ops {
static const size_t INDEX_IN_GRAD = 0;
static const size_t INDEX_IN_SUM_GRAD_R = 1;
static const size_t INDEX_IN_SUM_GRAD_C = 2;
static const size_t INDEX_IN_SUM_GRAD_RC = 3;
static const size_t INDEX_IN_R = 4;
static const size_t INDEX_IN_C = 5;

static const size_t INDEX_OUT_R = 0;
static const size_t INDEX_OUT_C = 1;
static const size_t INDEX_OUT_U = 2;
static const size_t INDEX_OUT_SUM_SQUARE_U = 3;

static const size_t MAX_DIM_NUM = 2;

template <typename T>
static std::string ConcatString(const T& arg)
{
    std::ostringstream oss;
    oss << arg;
    return oss.str();
}

template <typename T, typename... Ts>
static std::string ConcatString(const T& arg, const Ts&... arg_left)
{
    std::ostringstream oss;
    oss << arg;
    oss << ConcatString(arg_left...);
    return oss.str();
}

static ge::graphStatus CopyShapeInput2OutputWithIdxInApply(gert::InferShapeContext* context, int64_t input_idx,
                                                           int64_t output_idx)
{
    auto in_shape = context->GetInputShape(input_idx);
    OP_CHECK_NULL_WITH_CONTEXT(context, in_shape);
    auto out_shape = context->GetOutputShape(output_idx);
    OP_CHECK_NULL_WITH_CONTEXT(context, out_shape);
    *out_shape = *in_shape;
    return ge::GRAPH_SUCCESS;
}

static ge::graphStatus Infershape4ApplyCamePart2(gert::InferShapeContext* context)
{
    OP_LOGD(context, "Begin ApplyCamePart2Infershape.");
    const gert::Shape* gradShape = context->GetInputShape(INDEX_IN_GRAD);
    OP_CHECK_NULL_WITH_CONTEXT(context, gradShape);
    size_t gradDimNum = gradShape->GetDimNum();
    OP_CHECK_IF(
        gradDimNum != MAX_DIM_NUM,
        OP_LOGE(context->GetNodeName(), "%s",
                ConcatString("Infershape failed. dim num:", gradDimNum, "of grad must be", MAX_DIM_NUM, ".").c_str()),
        return GRAPH_FAILED);

    OP_CHECK_IF(CopyShapeInput2OutputWithIdxInApply(context, INDEX_IN_GRAD, INDEX_OUT_U) != GRAPH_SUCCESS,
                OP_LOGE(context->GetNodeName(), "%s", "Infershape4ApplyCamePart2 failed!"), return GRAPH_FAILED);
    OP_CHECK_IF(CopyShapeInput2OutputWithIdxInApply(context, INDEX_IN_R, INDEX_OUT_R) != GRAPH_SUCCESS,
                OP_LOGE(context->GetNodeName(), "%s", "Infershape4ApplyCamePart2 failed!"), return GRAPH_FAILED);
    OP_CHECK_IF(CopyShapeInput2OutputWithIdxInApply(context, INDEX_IN_C, INDEX_OUT_C) != GRAPH_SUCCESS,
                OP_LOGE(context->GetNodeName(), "%s", "Infershape4ApplyCamePart2 failed!"), return GRAPH_FAILED);
    OP_CHECK_IF(
        CopyShapeInput2OutputWithIdxInApply(context, INDEX_IN_SUM_GRAD_RC, INDEX_OUT_SUM_SQUARE_U) != GRAPH_SUCCESS,
        OP_LOGE(context->GetNodeName(), "%s", "Infershape4ApplyCamePart2 failed!"), return GRAPH_FAILED);

    OP_LOGD(context, "End ApplyCamePart2Infershape.");
    return ge::GRAPH_SUCCESS;
}

IMPL_OP_INFERSHAPE(ApplyCamePart2).InferShape(Infershape4ApplyCamePart2);
} // namespace ops
