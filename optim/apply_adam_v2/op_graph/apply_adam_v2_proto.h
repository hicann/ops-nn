/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */
/* Generated By CANNBot */

#ifndef GE_OP_APPLY_ADAM_V2_PROTO_H
#define GE_OP_APPLY_ADAM_V2_PROTO_H

#include "graph/operator_reg.h"

namespace ge {
/**
 * @brief Applies the Adam optimization algorithm to update model parameters.
 * Supports both standard Adam and mBART Adam modes, with optional gradient clipping.
 *
 * @par Inputs:
 * @li var: An ND tensor of shape [d_0, d_1, ..., d_n]. Model weights to be updated.
 *     Must be one of the following types: float16, float32.
 * @li m: An ND tensor. Same type and shape as var. First moment estimate (mean of gradients).
 * @li v: An ND tensor. Same type and shape as var. Second moment estimate (mean of squared gradients).
 * @li lr: An ND tensor of shape [1]. Same type as var. Learning rate scalar.
 * @li beta1: An ND tensor of shape [1]. Same type as var. Exponential decay rate for first moment.
 * @li beta2: An ND tensor of shape [1]. Same type as var. Exponential decay rate for second moment.
 * @li epsilon: An ND tensor of shape [1]. Same type as var. Small constant for numerical stability.
 * @li grad: An ND tensor. Same type and shape as var. Gradient of the loss with respect to var.
 * @li max_grad_norm: An optional ND tensor of shape [1]. Same type as var. Gradient clipping threshold.
 * @li global_grad_norm: An ND tensor of shape [1]. Same type as var. Global gradient norm for clipping.
 * @li weight_decay: An ND tensor of shape [1]. Same type as var. Weight decay coefficient.
 * @li step_size: An optional ND tensor of shape [1]. Same type as var. Step size scaling factor (mBART mode).
 *
 * @par Attributes:
 * @li adam_mode: An optional string. Computation mode, either "adam" or "mbart_adam". Defaults to "adam".
 *
 * @par Outputs:
 * @li var: An ND tensor. Same type and shape as input var. Updated model weights.
 * @li m: An ND tensor. Same type and shape as input m. Updated first moment.
 * @li v: An ND tensor. Same type and shape as input v. Updated second moment.
 *
 * @par Third-party framework compatibility
 * @li Compatible with PyTorch's Adam optimizer
 * @li Compatible with TensorFlow's Adam optimizer
 */
REG_OP(ApplyAdamV2)
    .INPUT(var, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(m, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(v, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(lr, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(beta1, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(beta2, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(epsilon, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(grad, TensorType({DT_FLOAT, DT_FLOAT16}))
    .OPTIONAL_INPUT(max_grad_norm, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(global_grad_norm, TensorType({DT_FLOAT, DT_FLOAT16}))
    .INPUT(weight_decay, TensorType({DT_FLOAT, DT_FLOAT16}))
    .OPTIONAL_INPUT(step_size, TensorType({DT_FLOAT, DT_FLOAT16}))
    .OUTPUT(var, TensorType({DT_FLOAT, DT_FLOAT16}))
    .OUTPUT(m, TensorType({DT_FLOAT, DT_FLOAT16}))
    .OUTPUT(v, TensorType({DT_FLOAT, DT_FLOAT16}))
    .ATTR(adam_mode, String, "adam")
    .OP_END_FACTORY_REG(ApplyAdamV2)
} // namespace ge

#endif // GE_OP_APPLY_ADAM_V2_PROTO_H
