/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file inplace_apply_centered_rms_prop_proto.h
 * \brief InplaceApplyCenteredRMSProp GE IR 算子注册
 */
#ifndef OPS_OP_PROTO_INC_InplaceApplyCenteredRMSProp_H_
#define OPS_OP_PROTO_INC_InplaceApplyCenteredRMSProp_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
 *@brief Centered RMSProp optimizer step with in-place state update.
 *       epsilon is inside sqrt (TF-aligned): sqrt(ms - mg² + ε)
 *@par Inputs:
 * Nine inputs, including:
 * @li var: A ND Tensor. Must be one of: float16, float. Persistent state (in-place updated).
 * @li mg: A ND Tensor. Same shape/dtype as var. Mean gradient (in-place updated).
 * @li ms: A ND Tensor. Same shape/dtype as var. Mean square (in-place updated).
 * @li mom: A ND Tensor. Same shape/dtype as var. Momentum buffer (in-place updated).
 * @li lr: A scalar Tensor (rank 0 or [1]). Learning rate.
 * @li rho: A scalar Tensor (rank 0 or [1]). Decay rate (smoothing constant).
 * @li momentum: A scalar Tensor (rank 0 or [1]). Momentum factor.
 * @li epsilon: A scalar Tensor (rank 0 or [1]). Numerical stability constant (inside sqrt).
 * @li grad: A ND Tensor. Same shape/dtype as var. Gradient.
 *
 *@par Outputs:
 * Four outputs (in-place alias with corresponding inputs):
 * @li var: Updated weight (shares GM address with input var).
 * @li mg: Updated mean gradient (shares GM address with input mg).
 * @li ms: Updated mean square (shares GM address with input ms).
 * @li mom: Updated momentum buffer (shares GM address with input mom).
 *
 *@par Attributes:
 * @li use_locking: Bool, default false. Reserved for TF compatibility.
 *
 *@par Third-party framework compatibility
 * Compatible with TensorFlow ResourceApplyCenteredRMSProp (epsilon inside sqrt).
 */
REG_OP(InplaceApplyCenteredRMSProp)
    .INPUT(var, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(mg, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(ms, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(mom, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(lr, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(rho, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(momentum, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(epsilon, TensorType({DT_FLOAT16, DT_FLOAT}))
    .INPUT(grad, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(var, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(mg, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(ms, TensorType({DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(mom, TensorType({DT_FLOAT16, DT_FLOAT}))
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyCenteredRMSProp)

} // namespace ge

#endif // OPS_OP_PROTO_INC_InplaceApplyCenteredRMSProp_H_
