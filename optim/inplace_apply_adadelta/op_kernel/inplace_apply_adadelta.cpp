/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 */
/*!
 * \file inplace_apply_adadelta.cpp
 * \brief InplaceApplyAdadelta kernel entry (arch35)
 *
 * GE IR signature (REG_OP(InplaceApplyAdadelta)):
 *   Inputs  (7 Tensor): var, accum, accum_update, lr, rho, epsilon, grad
 *   Outputs (3 Tensor): var, accum, accum_update (三显式 inplace 输出，均 alias 对应 INPUT)
 *
 * V2 相对 V1 净变更（Kernel 入口）：入参增加 accum_out / accum_update_out 两个输出
 * GM_ADDR，透传给 op.Init。参数顺序：7 输入 → 3 输出 → workspace → tiling。
 *
 * Dtype is expanded from the op definition and injected as DTYPE_VAR by the
 * build system. TilingKey only selects BUFFER_MODE (0=single, 1=double).
 */

#include "arch35/inplace_apply_adadelta.h"

#ifdef __CCE_KT_TEST__
template <typename D_T_X, int BUFFER_MODE>
__global__ __aicore__ void inplace_apply_adadelta(GM_ADDR var, GM_ADDR accum, GM_ADDR accum_update, GM_ADDR lr,
                                                  GM_ADDR rho, GM_ADDR epsilon, GM_ADDR grad, GM_ADDR var_out,
                                                  GM_ADDR accum_out, GM_ADDR accum_update_out, GM_ADDR workspace,
                                                  GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(InplaceApplyAdadeltaTilingData);
    GET_TILING_DATA_WITH_STRUCT(InplaceApplyAdadeltaTilingData, tilingData, tiling);
    if (tilingData.totalNum == 0) {
        return;
    }
    NsInplaceApplyAdadelta::InplaceApplyAdadelta<D_T_X, BUFFER_MODE> op;
    op.Init(var, accum, accum_update, lr, rho, epsilon, grad, var_out, accum_out, accum_update_out, &tilingData);
    op.Process();
}
#else
template <int BUFFER_MODE>
__global__ __aicore__ void inplace_apply_adadelta(GM_ADDR var, GM_ADDR accum, GM_ADDR accum_update, GM_ADDR lr,
                                                  GM_ADDR rho, GM_ADDR epsilon, GM_ADDR grad, GM_ADDR var_out,
                                                  GM_ADDR accum_out, GM_ADDR accum_update_out, GM_ADDR workspace,
                                                  GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(InplaceApplyAdadeltaTilingData);
    GET_TILING_DATA_WITH_STRUCT(InplaceApplyAdadeltaTilingData, tilingData, tiling);
    if (tilingData.totalNum == 0) {
        return;
    }
    NsInplaceApplyAdadelta::InplaceApplyAdadelta<DTYPE_VAR, BUFFER_MODE> op;
    op.Init(var, accum, accum_update, lr, rho, epsilon, grad, var_out, accum_out, accum_update_out, &tilingData);
    op.Process();
}
#endif
