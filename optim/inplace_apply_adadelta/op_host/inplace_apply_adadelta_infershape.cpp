/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 */
/*!
 * \file inplace_apply_adadelta_infershape.cpp
 * \brief InplaceApplyAdadelta shape inference（V2：三显式 inplace 输出）
 *
 * Inputs:  var(0), accum(1), accum_update(2), lr(3), rho(4), epsilon(5), grad(6)
 * Outputs: var(0), accum(1), accum_update(2)
 *   三输出 shape 均 = var(0) shape（= grad.shape，四张量同形，spec outputs[].shape_rule）。
 *   V2 相对 V1 净变更：补充 accum(1) / accum_update(2) 两个输出 shape。
 */

#include "register/op_impl_registry.h"
#include "exe_graph/runtime/infer_shape_context.h"

using namespace ge;

namespace ops {

constexpr size_t INPUT_VAR_INDEX = 0;
constexpr size_t OUTPUT_VAR_INDEX = 0;
constexpr size_t OUTPUT_ACCUM_INDEX = 1;
constexpr size_t OUTPUT_ACCUM_UPDATE_INDEX = 2;

static ge::graphStatus InferShape4InplaceApplyAdadelta(gert::InferShapeContext* context)
{
    // Input(0) = var; all outputs share var shape (四张量同形，无广播)
    const gert::Shape* inputShape = context->GetInputShape(INPUT_VAR_INDEX);
    if (inputShape == nullptr) {
        return ge::GRAPH_FAILED;
    }

    // Output(0) = var
    gert::Shape* varOutShape = context->GetOutputShape(OUTPUT_VAR_INDEX);
    if (varOutShape == nullptr) {
        return ge::GRAPH_FAILED;
    }
    *varOutShape = *inputShape;

    // Output(1) = accum (V2 新增输出 shape)
    gert::Shape* accumOutShape = context->GetOutputShape(OUTPUT_ACCUM_INDEX);
    if (accumOutShape == nullptr) {
        return ge::GRAPH_FAILED;
    }
    *accumOutShape = *inputShape;

    // Output(2) = accum_update (V2 新增输出 shape)
    gert::Shape* accumUpdateOutShape = context->GetOutputShape(OUTPUT_ACCUM_UPDATE_INDEX);
    if (accumUpdateOutShape == nullptr) {
        return ge::GRAPH_FAILED;
    }
    *accumUpdateOutShape = *inputShape;

    return ge::GRAPH_SUCCESS;
}

IMPL_OP_INFERSHAPE(InplaceApplyAdadelta).InferShape(InferShape4InplaceApplyAdadelta);

} // namespace ops
