/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/*!
 * \file inplace_apply_rms_prop_proto.h
 * \brief InplaceApplyRMSProp 图模式算子原型定义
 *
 * 对齐 canndev `REG_OP(InplaceApplyRMSProp)`（nn_training_ops.h / ops_proto_legacy.h）：
 *   - OpType：`InplaceApplyRMSProp`
 *   - 8 输入 + 3 输出（var / ms / mom 显式 inplace alias）
 *   - 属性：`use_locking`（语义占位）
 *   - dtype：`TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16})`
 */
#ifndef OPS_OP_PROTO_INC_INPLACE_APPLY_RMS_PROP_H_
#define OPS_OP_PROTO_INC_INPLACE_APPLY_RMS_PROP_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
 * @brief Updates "var" according to the RMSProp algorithm.
 *
 *   ms_new  = rho * ms + (1 - rho) * grad^2
 *   mom_new = momentum * mom + lr * grad / sqrt(ms_new + epsilon)
 *   var_new = var - mom_new
 *
 * @par Inputs:
 *  8 Tensor inputs:
 *   @li var:       A Tensor of type float / float16 / bfloat16. Weight to update.
 *   @li ms:        A Tensor with the same shape/dtype as "var". Mean-square accumulator (in-place updated).
 *   @li mom:       A Tensor with the same shape/dtype as "var". Momentum accumulator (in-place updated).
 *   @li lr:        Scalar Tensor (shape = [1]) learning rate.
 *   @li rho:       Scalar Tensor (shape = [1]) decay rate in [0, 1).
 *   @li momentum:  Scalar Tensor (shape = [1]) momentum coefficient.
 *   @li epsilon:   Scalar Tensor (shape = [1]) numerical-stability constant. Values no greater than FLT_MIN
 *                  (including NaN) are evaluated as FLT_MIN.
 *   @li grad:      A Tensor, same shape/dtype as "var".
 *
 * @par Outputs:
 *   @li var: Updated var (in-place alias of input var).
 *   @li ms: Updated mean-square accumulator (in-place alias of input ms).
 *   @li mom: Updated momentum accumulator (in-place alias of input mom).
 *
 * @par Attributes:
 *   @li use_locking: An optional bool. Defaults to "false". Semantic placeholder.
 *
 * @par Third-party framework compatibility
 * Compatible with the RMSProp state-update semantics used by the training
 * optimizer family. Framework adapters must preserve all three outputs.
 */
REG_OP(InplaceApplyRMSProp)
    .INPUT(var, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(ms, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(mom, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(lr, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(rho, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(momentum, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(epsilon, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(grad, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(var, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(ms, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(mom, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyRMSProp)

} // namespace ge

#endif // OPS_OP_PROTO_INC_INPLACE_APPLY_RMS_PROP_H_
