/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/**
 * NOTE: Portions of this code were AI-generated and have been
 * technically reviewed for functional accuracy and security
 */
/*!
 * \file inplace_apply_rms_prop_def.cpp
 * \brief InplaceApplyRMSProp 算子定义（对齐 canndev REG_OP(InplaceApplyRMSProp)）
 *
 * 输入 (8): var, ms, mom, lr, rho, momentum, epsilon, grad
 * 输出 (3): var / ms / mom（三个显式 inplace 输出端口）
 * 属性 (1): use_locking (bool, default=false, 语义占位)
 *
 * 仅适配 Ascend950 (arch35 / DAV_3510)。
 */
#include "register/op_def_registry.h"

namespace ops {

namespace {
constexpr std::initializer_list<ge::DataType> kDtypes = {ge::DT_FLOAT, ge::DT_FLOAT16, ge::DT_BF16};
constexpr std::initializer_list<ge::Format> kFormats = {ge::FORMAT_ND, ge::FORMAT_ND, ge::FORMAT_ND};

void AddInput(OpDef& op, const char* name)
{
    op.Input(name).ParamType(REQUIRED).DataType(kDtypes).Format(kFormats).UnknownShapeFormat(kFormats).AutoContiguous();
}

void AddOutput(OpDef& op, const char* name)
{
    op.Output(name)
        .ParamType(REQUIRED)
        .DataType(kDtypes)
        .Format(kFormats)
        .UnknownShapeFormat(kFormats)
        .AutoContiguous();
}

void AddAicoreConfig(OpDef& op)
{
    OpAICoreConfig config;
    config.DynamicCompileStaticFlag(true)
        .DynamicFormatFlag(false)
        .DynamicRankSupportFlag(true)
        .DynamicShapeSupportFlag(true)
        .NeedCheckSupportFlag(false)
        .PrecisionReduceFlag(true);
    op.AICore().AddConfig("ascend950", config);
}
} // namespace

class InplaceApplyRMSProp : public OpDef {
public:
    explicit InplaceApplyRMSProp(const char* name) : OpDef(name)
    {
        for (const char* name : {"var", "ms", "mom", "lr", "rho", "momentum", "epsilon", "grad"}) {
            AddInput(*this, name);
        }
        for (const char* name : {"var", "ms", "mom"}) {
            AddOutput(*this, name);
        }
        this->Attr("use_locking").AttrType(OPTIONAL).Bool(false);
        AddAicoreConfig(*this);
    }
};
OP_ADD(InplaceApplyRMSProp);
} // namespace ops
