/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 *
 * Generated By CANNBot
 */

/*!
 * \file test_inplace_apply_rms_prop_infershape.cpp
 * \brief InferShape UT for InplaceApplyRMSProp operator.
 *
 * The tests cover regular, empty, boundary, unknown-rank and scalar shapes.
 */

#include <gtest/gtest.h>
#include <iostream>
#include <vector>
#include "infershape_test_util.h"
#include "ut_op_common.h"
#include "../../../op_graph/inplace_apply_rms_prop_proto.h"

class InplaceApplyRMSProp : public testing::Test {
protected:
    static void SetUpTestCase() { std::cout << "InplaceApplyRMSProp SetUp" << std::endl; }

    static void TearDownTestCase() { std::cout << "InplaceApplyRMSProp TearDown" << std::endl; }
};

static gert::StorageShape MakeStorageShape(const std::vector<int64_t>& dims)
{
    gert::StorageShape shape;
    for (auto dim : dims) {
        shape.MutableOriginShape().AppendDim(dim);
        shape.MutableStorageShape().AppendDim(dim);
    }
    return shape;
}

static std::vector<int64_t> ShapeToVec(const gert::Shape* s)
{
    std::vector<int64_t> v;
    if (s == nullptr)
        return v;
    for (size_t i = 0; i < s->GetDimNum(); ++i) {
        v.push_back(s->GetDim(i));
    }
    return v;
}

static ge::graphStatus RunAndGetOutputShape(gert::StorageShape& tensorShape, gert::StorageShape& scalarShape,
                                            std::vector<int64_t>& outputDims)
{
    auto* opImpl = gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp");
    if (opImpl == nullptr) {
        return ge::GRAPH_FAILED;
    }
    auto inferShapeFunc = opImpl->infer_shape;
    if (inferShapeFunc == nullptr) {
        return ge::GRAPH_FAILED;
    }

    gert::StorageShape outputShape = {{}, {}};
    auto holder = gert::InferShapeContextFaker()
                      .NodeIoNum(8, 3)
                      .IrInstanceNum({1, 1, 1, 1, 1, 1, 1, 1})
                      .InputShapes({&tensorShape, &tensorShape, &tensorShape, &scalarShape, &scalarShape, &scalarShape,
                                    &scalarShape, &tensorShape})
                      .OutputShapes({&outputShape, &outputShape, &outputShape})
                      .Build();

    auto* ctx = holder.GetContext<gert::InferShapeContext>();
    auto status = inferShapeFunc(ctx);
    if (status == ge::GRAPH_SUCCESS) {
        outputDims = ShapeToVec(ctx->GetOutputShape(0));
    }
    return status;
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_0)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{96, 256}, {96, 256}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 2u);
    EXPECT_EQ(outDims[0], 96);
    EXPECT_EQ(outDims[1], 256);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_1)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{16}, {16}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 1u);
    EXPECT_EQ(outDims[0], 16);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_2)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{2, 4, 8, 16}, {2, 4, 8, 16}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 4u);
    EXPECT_EQ(outDims[0], 2);
    EXPECT_EQ(outDims[3], 16);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_3_empty)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{0}, {0}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 1u);
    EXPECT_EQ(outDims[0], 0);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_3_empty_axes)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    const std::vector<std::vector<int64_t>> emptyShapes = {{0, 4}, {4, 0}, {2, 0, 3}, {0, 2, 0}};
    for (const auto& dims : emptyShapes) {
        gert::StorageShape varShape = MakeStorageShape(dims);
        gert::StorageShape scalarShape = MakeStorageShape({1});
        std::vector<int64_t> outDims;
        ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
        EXPECT_EQ(outDims, dims);
    }
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_4_8d)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{2, 2, 2, 2, 2, 2, 2, 2}, {2, 2, 2, 2, 2, 2, 2, 2}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    EXPECT_EQ(outDims.size(), 8u);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_5_rank1)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{1}, {1}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 1u);
    EXPECT_EQ(outDims[0], 1);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_6_3d)
{
    ASSERT_NE(gert::OpImplRegistry::GetInstance().GetOpImpl("InplaceApplyRMSProp"), nullptr);
    gert::StorageShape varShape = {{8, 32, 64}, {8, 32, 64}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 3u);
    EXPECT_EQ(outDims[0], 8);
    EXPECT_EQ(outDims[1], 32);
    EXPECT_EQ(outDims[2], 64);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_7_unknown_rank)
{
    gert::StorageShape unknownRank = {{-2}, {-2}};
    gert::StorageShape scalarShape = {{1}, {1}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(unknownRank, scalarShape, outDims), ge::GRAPH_SUCCESS);
    ASSERT_EQ(outDims.size(), 1u);
    EXPECT_EQ(outDims[0], -2);
}

TEST_F(InplaceApplyRMSProp, InplaceApplyRMSProp_infershape_case_8_rank0)
{
    gert::StorageShape varShape = {{}, {}};
    gert::StorageShape scalarShape = {{}, {}};
    std::vector<int64_t> outDims;

    ASSERT_EQ(RunAndGetOutputShape(varShape, scalarShape, outDims), ge::GRAPH_SUCCESS);
    EXPECT_TRUE(outDims.empty());
}
