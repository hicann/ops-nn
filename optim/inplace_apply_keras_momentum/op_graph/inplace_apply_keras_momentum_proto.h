/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file inplace_apply_keras_momentum_proto.h
 * \brief InplaceApplyKerasMomentum GE IR 原型注册（双输出 inplace 语义）
 *
 * 与 ops-nn 现役 ApplyKerasMomentum（V1 单输出）相比：
 *   - V2 新增 OUTPUT(accum)：accum 跨训练步骤状态变量显式输出
 *   - inplace 语义：var/accum 输出与同名输入共享 Device 内存
 *   - 算子名改为 InplaceApplyKerasMomentum
 */

#ifndef OPS_OP_PROTO_INC_INPLACEAPPLYKERASMOMENTUM_H_
#define OPS_OP_PROTO_INC_INPLACEAPPLYKERASMOMENTUM_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {
/**
 *@brief Updates '*var' according to the Keras momentum scheme (inplace dual output).
 *   accum_new = accum * momentum - grad * lr
 *   if use_nesterov is True:
 *       var += momentum * accum_new - grad * lr
 *   else:
 *       var += accum_new
 *   accum = accum_new
 *
 *@par Inputs:
 *@li var: A mutable tensor. Must be one of FLOAT32/FLOAT16/BF16. Should be from a Variable().
 *@li accum: A mutable tensor. Has the same type as "var". Should be from a Variable().
 *@li lr: A scalar tensor of FLOAT32. Learning rate.
 *@li grad: A tensor for the gradient. Has the same type as "var".
 *@li momentum: A scalar tensor of FLOAT32. Momentum coefficient.
 *
 *@par Attributes:
 *@li use_locking: An optional bool. Defaults to "False".
 *    If "True", updating of the "var" tensor is protected by a lock;
 *    NPU 单流执行忽略，保留属性兼容 TF ResourceApplyKerasMomentum。
 *@li use_nesterov: An optional bool. Defaults to "False".
 *    If "True", var will be updated by using Nesterov momentum.
 *
 *@par Outputs:
 *@li var: A mutable tensor. Has the same type as input "var". inplace_with(var).
 *@li accum: A mutable tensor. Has the same type as input "accum". inplace_with(accum).
 *
 *@attention Constraints:
 * The input tensors var/accum/grad must have the same shape.
 *
 *@par Third-party framework compatibility
 * Compatible with the TensorFlow operator ResourceApplyKerasMomentum.
 *
 */

REG_OP(InplaceApplyKerasMomentum)
    .INPUT(var, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(accum, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(lr, TensorType({DT_FLOAT}))
    .INPUT(grad, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(momentum, TensorType({DT_FLOAT}))
    .OUTPUT(var, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(accum, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .ATTR(use_locking, Bool, false)
    .ATTR(use_nesterov, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyKerasMomentum)

} // namespace ge

#endif // OPS_OP_PROTO_INC_INPLACEAPPLYKERASMOMENTUM_H_
