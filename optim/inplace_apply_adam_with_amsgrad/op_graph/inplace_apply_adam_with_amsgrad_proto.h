/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file inplace_apply_adam_with_amsgrad_proto.h
 * \brief InplaceApplyAdamWithAmsgrad 算子 GE IR 原型定义（图模式）。
 *
 * Adam 优化器带 AMSGrad 修正的原地（in-place）参数更新算子，对标 TensorFlow
 * tf.raw_ops.ResourceApplyAdamWithAmsgrad。逐元素更新参数与一阶矩、二阶矩、
 * 二阶矩历史最大值状态，var/m/v/vhat 四个状态张量原地写回。
 */
#ifndef OPS_OP_PROTO_INC_INPLACE_APPLY_ADAM_WITH_AMSGRAD_H_
#define OPS_OP_PROTO_INC_INPLACE_APPLY_ADAM_WITH_AMSGRAD_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
 *@brief Inplace Adam update with AMSGrad correction.
 *  lr = learning_rate * (sqrt(1 - beta2_power)) / (1 - beta1_power)
 *  m = m + (1 - beta1) * (grad - m)
 *  v = v + (1 - beta2) * (grad * grad - v)
 *  vhat = max(vhat, v)
 *  var = var - lr * m / (epsilon + sqrt(vhat))
 *
 *@attention Constraints:
 *  *The five element-wise tensors var/m/v/vhat/grad must have the same shape.*
 *
 *@par Inputs:
 *@li var: A mutable Tensor of type {DT_BF16, DT_FLOAT16, DT_FLOAT}. Should be from a Variable().
 *@li m: A mutable Tensor of the same type as "var". Should be from a Variable().
 *@li v: A mutable Tensor of the same type as "var". Should be from a Variable().
 *@li vhat: A mutable Tensor of the same type as "var". Should be from a Variable().
 *@li beta1_power: A scalar of type DT_FLOAT.
 *@li beta2_power: A scalar of type DT_FLOAT.
 *@li lr: learning_rate. A scalar of type DT_FLOAT.
 *@li beta1: A scalar of type DT_FLOAT.
 *@li beta2: A scalar of type DT_FLOAT.
 *@li epsilon: A scalar of type DT_FLOAT.
 *@li grad: A Tensor of the same type as "var", for the gradient.
 *
 *@par Attributes:
 *@li use_locking: An optional bool. Defaults to "False".
 *     If "True", updating of the var, m, v and vhat tensors will be protected
 *     by a lock; otherwise the behavior is undefined, but may exhibit less
 *     contention.
 *
 *@par Outputs:
 *@li var: Same shape and dtype as input "var", in-place updated.
 *@li m: Same shape and dtype as input "m", in-place updated.
 *@li v: Same shape and dtype as input "v", in-place updated.
 *@li vhat: Same shape and dtype as input "vhat", in-place updated.
 *
 *@par Third-party framework compatibility
 *Compatible with the TensorFlow operator ResourceApplyAdamWithAmsgrad.
 */
#ifndef OPS_PROTO_DEF_INPLACEAPPLYADAMWITHAMSGRAD
#define OPS_PROTO_DEF_INPLACEAPPLYADAMWITHAMSGRAD
REG_OP(InplaceApplyAdamWithAmsgrad)
    .INPUT(var, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(m, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(v, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(vhat, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .INPUT(beta1_power, TensorType({DT_FLOAT}))
    .INPUT(beta2_power, TensorType({DT_FLOAT}))
    .INPUT(lr, TensorType({DT_FLOAT}))
    .INPUT(beta1, TensorType({DT_FLOAT}))
    .INPUT(beta2, TensorType({DT_FLOAT}))
    .INPUT(epsilon, TensorType({DT_FLOAT}))
    .INPUT(grad, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(var, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(m, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(v, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .OUTPUT(vhat, TensorType({DT_BF16, DT_FLOAT16, DT_FLOAT}))
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyAdamWithAmsgrad)
#endif

} // namespace ge

#endif // OPS_OP_PROTO_INC_INPLACE_APPLY_ADAM_WITH_AMSGRAD_H_
