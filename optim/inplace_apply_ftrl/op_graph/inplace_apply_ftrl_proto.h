/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file inplace_apply_ftrl_proto.h
 * \brief InplaceApplyFtrl GE IR 原型注册
 */
#ifndef OPS_NN_OPTIM_INPLACE_APPLY_FTRL_PROTO_H
#define OPS_NN_OPTIM_INPLACE_APPLY_FTRL_PROTO_H

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {
/**
 * @brief Updates "var" according to the FTRL-Proximal scheme.
 *  computing process:
 *    accum_new = accum + grad * grad
 *    pow_new = accum_new ^ (-lr_power)
 *    linear_new = linear + grad - (pow_new - pow_old) / lr * var
 *    var_new = (|linear_new| > l1) ? (sign(linear_new) * l1 - linear_new) / (pow_new / lr + 2 * l2) : 0
 *
 * @attention Constraints:
 *  All input tensors must have the same shape.
 *
 * @par Inputs:
 * @li var: A mutable tensor. Should be from a Variable().
 * @li accum: A mutable tensor. Has the same type as "var".
 * @li linear: A mutable tensor. Has the same type as "var".
 * @li grad: A tensor for the gradient. Has the same type as "var".
 * @li lr: A scalar. Has the same type as "var".
 * @li l1: A scalar. Has the same type as "var".
 * @li l2: A scalar. Has the same type as "var".
 * @li lr_power: A scalar. Has the same type as "var".
 *
 * @par Attributes:
 * @li use_locking: An optional bool. Defaults to "False".
 *
 * @par Outputs:
 * @li var: A mutable tensor. Has the same type as input "var".
 * @li accum: A mutable tensor. Has the same type as input "accum".
 * @li linear: A mutable tensor. Has the same type as input "linear".
 *
 * @par Third-party framework compatibility
 * Compatible with the TensorFlow operator ApplyFtrl.
 *
 * @par Restrictions:
 * None.
 */

REG_OP(InplaceApplyFtrl)
    .INPUT(var, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(accum, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(linear, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(grad, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(lr, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(l1, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(l2, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .INPUT(lr_power, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(var, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(accum, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .OUTPUT(linear, TensorType({DT_FLOAT, DT_FLOAT16, DT_BF16}))
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(InplaceApplyFtrl)

} // namespace ge

#endif // OPS_NN_OPTIM_INPLACE_APPLY_FTRL_PROTO_H
