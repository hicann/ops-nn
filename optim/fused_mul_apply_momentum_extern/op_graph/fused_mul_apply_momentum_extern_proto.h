/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file fused_mul_apply_momentum_extern_proto.h
 * \brief GE IR 图模式原型（REG_OP，7 输入 / 3 输出 + use_nesterov/use_locking 属性）
 *
 * CANN 独有图模式融合训练算子，由 momentum_lossscale_fusion_pass 融合 PASS 产生。
 * 三输出 inplace 复用对应输入名（var↔var、var_copy↔var_copy、accum↔accum，GM 地址复用）。
 */
#ifndef OPS_OP_PROTO_INC_FUSED_MUL_APPLY_MOMENTUM_EXTERN_H_
#define OPS_OP_PROTO_INC_FUSED_MUL_APPLY_MOMENTUM_EXTERN_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
 *@brief Fused Mul + ApplyMomentum training op (CANN graph-mode).
 *  grad = x1 * x2; accum' = accum * momentum + grad;
 *  var_delta = accum' * lr (standard) or grad*lr + accum'*momentum*lr (Nesterov);
 *  var' = var - var_delta; var_copy' = var_copy - Cast(var_delta) (independent low-precision copy).
 *@par Inputs:
 * @li var: master weight, FP32, in-place updated.
 * @li accum: momentum buffer.
 * @li lr: learning rate scalar.
 * @li x1: gradient component g1.
 * @li momentum: momentum coefficient scalar.
 * @li x2: LossScale reciprocal g2 scalar.
 * @li var_copy: low-precision weight copy (FP16/BF16), in-place updated.
 *@par Outputs:
 * @li var: updated master weight var' (in-place, reuses input var).
 * @li var_copy: updated low-precision copy var_copy' (in-place, reuses input var_copy).
 * @li accum: updated momentum buffer accum' (in-place, reuses input accum).
 *@par Attributes:
 * @li use_nesterov: bool, default false. Standard/Nesterov var_delta.
 * @li use_locking: bool, default false. Reserved (no numerical effect).
 */
REG_OP(FusedMulApplyMomentumExtern)
    .INPUT(var, TensorType(DT_FLOAT))
    .INPUT(accum, TensorType::NumberType())
    .INPUT(lr, TensorType::NumberType())
    .INPUT(x1, TensorType::NumberType())
    .INPUT(momentum, TensorType::NumberType())
    .INPUT(x2, TensorType::NumberType())
    .INPUT(var_copy, TensorType(DT_FLOAT16))
    .OUTPUT(var, TensorType(DT_FLOAT))
    .OUTPUT(var_copy, TensorType(DT_FLOAT16))
    .OUTPUT(accum, TensorType::NumberType())
    .ATTR(use_nesterov, Bool, false)
    .ATTR(use_locking, Bool, false)
    .OP_END_FACTORY_REG(FusedMulApplyMomentumExtern)

} // namespace ge

#endif // OPS_OP_PROTO_INC_FUSED_MUL_APPLY_MOMENTUM_EXTERN_H_
