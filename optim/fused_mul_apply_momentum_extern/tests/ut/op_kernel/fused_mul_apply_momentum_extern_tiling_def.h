/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file fused_mul_apply_momentum_extern_tiling_def.h
 * \brief op_kernel UT 用 TilingData 复刻（EleWise 线性切分，与 kernel 侧
 *        op_kernel/arch35/fused_mul_apply_momentum_extern_tiling_data.h 逐字段对齐）。
 *
 * AddOpTestCase 通过 -include 强制预置本文件到 kernel/测试 TU；本文件复用与真实
 * tiling_data.h 相同的头保护宏 FUSED_MUL_APPLY_MOMENTUM_EXTERN_TILING_DATA_H，
 * 使 kernel 头链后续 include 真实头时被跳过，避免结构体重定义。
 */

#ifndef FUSED_MUL_APPLY_MOMENTUM_EXTERN_TILING_DATA_H
#define FUSED_MUL_APPLY_MOMENTUM_EXTERN_TILING_DATA_H

#include "kernel_tiling/kernel_tiling.h"

struct FusedMulApplyMomentumExternTilingData {
    int64_t dim0 = 0;                // 元素总数（四张量同 shape，所有维度相乘）
    int64_t coreNum = 0;             // 实际使用核数
    int64_t blockFormer = 0;         // 每核基础元素数（512 元素对齐）
    int64_t blockNum = 0;            // 虚拟 block 数量
    int64_t ubFormer = 0;            // 每个 UB 块基础元素数（256B 对齐）
    int64_t ubLoopOfFormerBlock = 0; // 首 block 的 UB 循环次数
    int64_t ubTailOfFormerBlock = 0; // 首 block 的尾部元素数
    int64_t ubLoopOfTailBlock = 0;   // 尾 block 的 UB 循环次数
    int64_t ubTailOfTailBlock = 0;   // 尾 block 的尾部元素数
};

// CPU UT（tikicpulib）下 tiling 头由本文件替代自动生成，故补齐 kernel 入口所用
// GET_TILING_DATA_WITH_STRUCT 宏。tikicpulib 的 GM 为宿主内存，直接按结构体拷贝即可。
// 本文件仅在 op_kernel UT 编译（AddOpTestCase 的 -include）中生效，不影响 NPU 生产构建。
#ifndef GET_TILING_DATA_WITH_STRUCT
#define GET_TILING_DATA_WITH_STRUCT(tiling_struct, tiling_data, tiling_arg) \
    tiling_struct tiling_data = *reinterpret_cast<const tiling_struct*>(tiling_arg)
#endif

#endif
