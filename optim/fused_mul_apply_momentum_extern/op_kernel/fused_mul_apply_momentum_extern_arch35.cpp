/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file fused_mul_apply_momentum_extern_arch35.cpp
 * \brief FusedMulApplyMomentumExtern kernel 入口（arch35 架构）
 *
 * 核函数参数顺序契约：输入(7) → 输出(3) → workspace → tiling。
 * 模板参数 USE_NESTEROV 与 tiling_key.h 的 ASCENDC_TPL_ARGS_DECL 对应。
 * dtype 由 def 文件 DataType 列驱动，构建系统注入 DTYPE_* 编译宏
 *   （ComputeT=DTYPE_ACCUM，VarCopyT=DTYPE_VAR_COPY）。
 */

#include "arch35/fused_mul_apply_momentum_extern.h"

// dtype 宏兜底（构建系统按 def DataType 列注入；缺失时按 K0 FP32 通路取默认）
#ifndef DTYPE_ACCUM
#define DTYPE_ACCUM float
#endif
#ifndef DTYPE_VAR_COPY
#define DTYPE_VAR_COPY half
#endif

#ifdef __CCE_KT_TEST__
// CPU UT（tikicpulib）入口：非模板 extern "C" 符号，供 ICPU_RUN_KF 链接调用。
//   NPU 上 USE_NESTEROV 由 ASCENDC_TPL_SEL 编译期特化（Host TilingKey 0=标准 / 1=Nesterov）；
//   CPU UT 沿用同一 TilingKey 语义，两模板变体均实例化，运行期由 ICPU_SET_TILING_KEY 分派：
//     TilingKey 0 → USE_NESTEROV=0（标准动量）
//     TilingKey 1 → USE_NESTEROV=1（Nesterov）
extern "C" __global__ __aicore__ void fused_mul_apply_momentum_extern(GM_ADDR var, GM_ADDR accum, GM_ADDR lr,
                                                                      GM_ADDR x1, GM_ADDR momentum, GM_ADDR x2,
                                                                      GM_ADDR var_copy, GM_ADDR var_out,
                                                                      GM_ADDR var_copy_out, GM_ADDR accum_out,
                                                                      GM_ADDR workspace, GM_ADDR tiling)
{
    GET_TILING_DATA_WITH_STRUCT(FusedMulApplyMomentumExternTilingData, tilingData, tiling);

    if (TILING_KEY_IS(1)) {
        NsFusedMulApplyMomentumExtern::FusedMulApplyMomentumExtern<DTYPE_ACCUM, DTYPE_VAR_COPY, 1> op;
        op.Init(var, accum, lr, x1, momentum, x2, var_copy, var_out, var_copy_out, accum_out, &tilingData);
        op.Process();
    } else {
        NsFusedMulApplyMomentumExtern::FusedMulApplyMomentumExtern<DTYPE_ACCUM, DTYPE_VAR_COPY, 0> op;
        op.Init(var, accum, lr, x1, momentum, x2, var_copy, var_out, var_copy_out, accum_out, &tilingData);
        op.Process();
    }
}
#else
template <int USE_NESTEROV>
__global__ __aicore__ void fused_mul_apply_momentum_extern(GM_ADDR var, GM_ADDR accum, GM_ADDR lr, GM_ADDR x1,
                                                           GM_ADDR momentum, GM_ADDR x2, GM_ADDR var_copy,
                                                           GM_ADDR var_out, GM_ADDR var_copy_out, GM_ADDR accum_out,
                                                           GM_ADDR workspace, GM_ADDR tiling)
{
    REGISTER_TILING_DEFAULT(FusedMulApplyMomentumExternTilingData);
    GET_TILING_DATA_WITH_STRUCT(FusedMulApplyMomentumExternTilingData, tilingData, tiling);

    NsFusedMulApplyMomentumExtern::FusedMulApplyMomentumExtern<DTYPE_ACCUM, DTYPE_VAR_COPY, USE_NESTEROV> op;
    op.Init(var, accum, lr, x1, momentum, x2, var_copy, var_out, var_copy_out, accum_out, &tilingData);
    op.Process();
}
#endif
